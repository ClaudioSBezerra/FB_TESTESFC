---
phase: quick-260702-ju0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/pages/Login.tsx]
autonomous: true
requirements: [QUICK-ju0]
must_haves:
  truths:
    - "A tela de login exibe exatamente um item: 'Ler base atual de saídas e fazer cálculos para reforma tributária'"
    - "Nenhum dos 5 textos herdados do FB_APU04 (EFD/SPED/dashboards) aparece mais na tela"
  artifacts:
    - path: "frontend/src/pages/Login.tsx"
      provides: "Array FEATURES com um único item que reflete o propósito do FB_TESTESFC"
      contains: "Ler base atual de saídas e fazer cálculos para reforma tributária"
  key_links: []
---

<objective>
Substituir o array `FEATURES` na tela de login (herdado do FB_APU04, com 5 itens sobre EFD/SPED/apuração fiscal) por um único item que reflete o que o FB_TESTESFC realmente faz: validar o pacote fiscal recalculando impostos da base de saídas.

Purpose: Os bullets atuais descrevem funcionalidades do projeto irmão (apuração/EFD) que não existem neste projeto — mensagem enganosa na primeira tela que o usuário vê.
Output: `frontend/src/pages/Login.tsx` com o array `FEATURES` de um único item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@frontend/src/pages/Login.tsx

<interfaces>
Estado atual (frontend/src/pages/Login.tsx, linhas 13-19):

```typescript
const FEATURES = [
  "Importação de várias fontes: EFD ICMS, XMLs e planilhas",
  "Importação de NF-e via ERP Bridge com PIS, COFINS e IPI",
  "Dados escriturados visualizados em dashboards",
  "Rastreabilidade NF-e → lançamento EFD para auditoria fiscal",
  "Conformidade SPED layout 020 (vigência Janeiro/2026)",
];
```

O array é apenas renderizado (map) na tela — nenhuma lógica de componente depende do número de itens.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir array FEATURES por item único</name>
  <files>frontend/src/pages/Login.tsx</files>
  <action>Substituir integralmente o literal do array `FEATURES` (linhas 13-19) por um array de um único elemento contendo exatamente a string "Ler base atual de saídas e fazer cálculos para reforma tributária". Remover os 5 itens herdados sobre EFD/SPED/dashboards. Não alterar nenhuma outra parte do arquivo — nome da constante, imports e a lógica de renderização permanecem intactos.</action>
  <verify>
    <automated>cd frontend && grep -c "Ler base atual de saídas e fazer cálculos para reforma tributária" src/pages/Login.tsx | grep -q '^1$' && ! grep -qE 'EFD|SPED|dashboards|Rastreabilidade' src/pages/Login.tsx && npx tsc --noEmit -p . 2>&1 | grep -v 'Login.tsx' | grep -qE 'error TS' && echo FAIL || echo OK</automated>
  </verify>
  <done>O array FEATURES contém exatamente um item com o texto especificado; nenhum texto herdado (EFD/SPED/dashboards/Rastreabilidade) permanece; o arquivo continua compilando (tsc sem erros novos em Login.tsx).</done>
</task>

</tasks>

<verification>
- `frontend/src/pages/Login.tsx` tem o array `FEATURES` com um único item.
- Texto exato: "Ler base atual de saídas e fazer cálculos para reforma tributária".
- Nenhum resquício dos 5 textos antigos.
</verification>

<success_criteria>
- Ao abrir a tela de login, apenas o novo bullet aparece.
- Build do frontend não quebra.
</success_criteria>

<output>
Create `.planning/quick/260702-ju0-na-tela-de-login-trocar-a-lista-de-bulle/260702-ju0-SUMMARY.md` when done
</output>
