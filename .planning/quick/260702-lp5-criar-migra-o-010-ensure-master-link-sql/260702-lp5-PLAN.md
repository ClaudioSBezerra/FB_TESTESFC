---
phase: quick
plan: 260702-lp5
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/010_ensure_master_link.sql
  - ~/.claude/skills/coolify-deploy-checklist/SKILL.md
autonomous: true
requirements: [QUICK-LP5]

must_haves:
  truths:
    - "A próxima subida do backend (local ou produção) cria/garante Ambiente, Grupo e Empresa 'MASTER' vinculados ao admin claudio_bezerra@hotmail.com"
    - "Rodar a migração duas vezes não duplica registros (idempotente)"
    - "A skill global coolify-deploy-checklist documenta o padrão MASTER como item numerado do checklist de novo produto"
  artifacts:
    - path: "backend/migrations/010_ensure_master_link.sql"
      provides: "Migração idempotente que garante hierarquia MASTER e vínculo do admin"
      contains: "DO $$"
    - path: "~/.claude/skills/coolify-deploy-checklist/SKILL.md"
      provides: "Seção 1.7 documentando o padrão de migração MASTER"
      contains: "ensure_master_link"
  key_links:
    - from: "backend/migrations/010_ensure_master_link.sql"
      to: "backend/migrations/004_seed_ferreira_costa.sql"
      via: "usuário claudio_bezerra@hotmail.com criado em 004, referenciado em 010"
      pattern: "claudio_bezerra@hotmail.com"
---

<objective>
Criar a migração `backend/migrations/010_ensure_master_link.sql` no FB_TESTESFC, portando quase literalmente a migração de referência `024_ensure_master_link.sql` do FB_APU04. A produção subiu com o banco sem um ambiente/tenant padrão vinculado ao admin, então o login funciona mas não há ambiente selecionável. A migração garante — de forma idempotente — que a hierarquia MASTER (Ambiente → Grupo → Empresa) existe e está vinculada ao admin `claudio_bezerra@hotmail.com`.

Também documentar esse padrão na skill global `coolify-deploy-checklist` para que produtos futuros da família FBTax nasçam com essa migração e evitem o mesmo problema no primeiro deploy com banco zerado.

Purpose: Eliminar o estado "logado mas sem tenant" logo após o primeiro deploy em produção, e capturar o padrão como conhecimento reutilizável.
Output: 1 arquivo de migração SQL + 1 seção nova na skill global.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Schema confirmado em backend/migrations/001_auth_hierarchy.sql — compatível 1:1 com a migração de referência do FB_APU04. Executor NÃO precisa explorar o schema. -->

environments        (id UUID PK, name VARCHAR NOT NULL, description TEXT, ...)
enterprise_groups   (id UUID PK, environment_id UUID FK NOT NULL, name VARCHAR NOT NULL, description TEXT, ...)
companies           (id UUID PK, group_id UUID FK NOT NULL, owner_id UUID, name VARCHAR NOT NULL, trade_name VARCHAR, ...)
users               (id UUID PK, email VARCHAR UNIQUE, ...)   -- claudio_bezerra@hotmail.com já inserido em 004_seed_ferreira_costa.sql
user_environments   (user_id UUID FK, environment_id UUID FK, role VARCHAR DEFAULT 'admin', preferred_company_id UUID, PK (user_id, environment_id))
</interfaces>

<reference>
<!-- Migração canônica a portar quase literalmente: -->
/home/claudiobezerra/projetos/FB_APU04/backend/migrations/024_ensure_master_link.sql
</reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migração 010_ensure_master_link.sql</name>
  <files>backend/migrations/010_ensure_master_link.sql</files>
  <action>
Criar `backend/migrations/010_ensure_master_link.sql` portando a lógica de `/home/claudiobezerra/projetos/FB_APU04/backend/migrations/024_ensure_master_link.sql` quase literalmente (o schema é compatível 1:1 — ver `<interfaces>`).

Estrutura obrigatória (idêntica à referência):
- Cabeçalho de comentário: `-- Migration: Ensure MASTER hierarchy and Link Admin User`, data `-- Created: 2026-07-02`, e descrição mencionando que garante a estrutura MASTER e vincula claudio_bezerra@hotmail.com.
- Bloco `DO $$` com `DECLARE` de `v_user_id`, `v_env_id`, `v_group_id`, `v_company_id` (todos UUID) e `BEGIN ... END $$;`.
- Passo 1: `SELECT id INTO v_user_id FROM users WHERE email = 'claudio_bezerra@hotmail.com';` — se `v_user_id IS NULL`, `RAISE NOTICE` de "não encontrado" e nenhuma ação (bloco `ELSE` no final).
- Passo 2: garantir Ambiente `SELECT ... FROM environments WHERE name = 'MASTER'`; se NULL, `INSERT INTO environments (name, description) VALUES ('MASTER', 'Ambiente Principal de Administração') RETURNING id INTO v_env_id;` com `RAISE NOTICE` criado/já existe.
- Passo 3: garantir Grupo `WHERE name = 'MASTER' AND environment_id = v_env_id`; se NULL, `INSERT INTO enterprise_groups (environment_id, name, description) VALUES (v_env_id, 'MASTER', 'Grupo Corporativo') RETURNING id INTO v_group_id;` com `RAISE NOTICE`.
- Passo 4: garantir Empresa `WHERE name = 'MASTER' AND group_id = v_group_id`; se NULL, `INSERT INTO companies (group_id, name, trade_name) VALUES (v_group_id, 'MASTER', 'MASTER Corporation') RETURNING id INTO v_company_id;` com `RAISE NOTICE`.
- Passo 5: vínculo `IF NOT EXISTS (SELECT 1 FROM user_environments WHERE user_id = v_user_id AND environment_id = v_env_id) THEN INSERT INTO user_environments (user_id, environment_id, role) VALUES (v_user_id, v_env_id, 'admin');` com `RAISE NOTICE`.

Manter os mesmos nomes/strings exatos: 'MASTER', 'Ambiente Principal de Administração', 'Grupo Corporativo', 'MASTER Corporation', role 'admin', email 'claudio_bezerra@hotmail.com'. Manter os `RAISE NOTICE` (padrão idempotente com log). NÃO rodar a migração — ela executa sozinha no próximo boot do backend.
  </action>
  <verify>
    <automated>test -f backend/migrations/010_ensure_master_link.sql && awk '/DO \$\$/{o++} /END \$\$;/{c++} END{if(o==1 \&\& c==1) print "BALANCED"; else {print "UNBALANCED o="o" c="c; exit 1}}' backend/migrations/010_ensure_master_link.sql && grep -c "MASTER" backend/migrations/010_ensure_master_link.sql</automated>
  </verify>
  <done>Arquivo existe; bloco `DO $$ ... END $$;` balanceado (1 abertura, 1 fechamento); contém as 4 strings 'MASTER' e o email do admin; lógica idempotente com RAISE NOTICE preservada da referência.</done>
</task>

<task type="auto">
  <name>Task 2: Documentar padrão MASTER na skill global coolify-deploy-checklist</name>
  <files>~/.claude/skills/coolify-deploy-checklist/SKILL.md</files>
  <action>
Editar `~/.claude/skills/coolify-deploy-checklist/SKILL.md` (arquivo GLOBAL, FORA do repositório git do FB_TESTESFC — NÃO commitar via git nesta task).

Inserir uma seção nova numerada `## 1.7. Migração de vínculo MASTER (ambiente padrão de administração)` IMEDIATAMENTE ANTES da seção existente `## 2. Depois do PRIMEIRO deploy em produção, verificar no servidor`. As seções 1, 1.5 e 1.6 já existem — a nova entra em sequência lógica como 1.7, ainda dentro do bloco "antes/durante o deploy".

Conteúdo da seção (prosa em pt-BR, no mesmo tom das demais):
- Explicar o problema observado: produtos dessa família (auth hierárquico Ambiente → Grupo → Empresa → user_environments) sobem em produção com banco zerado e, mesmo com o admin logando, não há ambiente/tenant selecionável — o usuário fica "logado sem tenant".
- Regra: todo produto novo dessa família deve incluir uma migração idempotente do tipo `NNN_ensure_master_link.sql` que garante Ambiente/Grupo/Empresa 'MASTER' e vincula o admin principal via `user_environments` (role 'admin'), usando o padrão `SELECT ... INTO / IF NULL THEN INSERT ... RETURNING` com `RAISE NOTICE` (roda sem duplicar em toda subida do backend).
- Referenciar `backend/migrations/024_ensure_master_link.sql` do FB_APU04 como o padrão canônico a copiar, e citar `backend/migrations/010_ensure_master_link.sql` do FB_TESTESFC como aplicação concreta desse padrão.
- Deixar claro que a migração roda sozinha no próximo boot do backend (mecanismo de migrations do projeto), não precisa execução manual.

Não alterar as demais seções nem o frontmatter.
  </action>
  <verify>
    <automated>grep -q "## 1.7." ~/.claude/skills/coolify-deploy-checklist/SKILL.md && grep -q "ensure_master_link" ~/.claude/skills/coolify-deploy-checklist/SKILL.md && awk '/## 1.7./{a=NR} /## 2\. Depois do PRIMEIRO deploy/{b=NR} END{if(a>0 \&\& b>0 \&\& a<b) print "ORDER_OK"; else {print "ORDER_BAD a="a" b="b; exit 1}}' ~/.claude/skills/coolify-deploy-checklist/SKILL.md</automated>
  </verify>
  <done>Seção `## 1.7.` existe no SKILL.md, posicionada antes de `## 2. Depois do PRIMEIRO deploy`; menciona `ensure_master_link` e referencia o padrão canônico do FB_APU04. Arquivo NÃO commitado (fora do repo).</done>
</task>

</tasks>

<verification>
- `backend/migrations/010_ensure_master_link.sql` existe, com `DO $$ ... END $$;` balanceado e as strings MASTER + email do admin.
- `~/.claude/skills/coolify-deploy-checklist/SKILL.md` tem a seção 1.7 antes da seção 2, referenciando o padrão de migração MASTER.
- Nenhuma execução contra banco real necessária — validação apenas de sintaxe/estrutura via grep/awk.
</verification>

<success_criteria>
- Migração criada, idempotente, pronta para rodar no próximo boot do backend (local ou produção).
- Padrão MASTER documentado na skill global como item numerado do checklist de novo produto.
- Task 1 commitada no git do FB_TESTESFC; Task 2 (skill global) NÃO commitada (fora do repositório).
</success_criteria>

<output>
Create `.planning/quick/260702-lp5-criar-migra-o-010-ensure-master-link-sql/260702-lp5-SUMMARY.md` when done
</output>
