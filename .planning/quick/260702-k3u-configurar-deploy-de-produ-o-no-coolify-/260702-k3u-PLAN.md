---
phase: quick-260702-k3u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.prod.yml
  - .github/workflows/deploy-production.yml
autonomous: true
requirements: [DEPLOY-PROD]
must_haves:
  truths:
    - "docker-compose.prod.yml existe e é YAML válido"
    - "Serviço db NÃO está na rede coolify (isolamento contra colisão de DNS entre stacks)"
    - "Apenas o serviço web está na rede coolify, com labels Traefik para testesfc.fbtax.cloud"
    - "Container da API mantém o nome fb_testesfc-api (nginx.conf do frontend resolve esse hostname)"
    - "Push em main dispara o webhook do Coolify via GitHub Actions"
  artifacts:
    - path: "docker-compose.prod.yml"
      provides: "Stack de produção api+web+db para Coolify"
      contains: "external: true"
    - path: ".github/workflows/deploy-production.yml"
      provides: "CI que dispara redeploy no Coolify via webhook"
      contains: "COOLIFY_WEBHOOK_URL"
  key_links:
    - from: "docker-compose.prod.yml web service"
      to: "Traefik (rede coolify)"
      via: "labels traefik.http.routers Host(testesfc.fbtax.cloud)"
      pattern: "testesfc\\.fbtax\\.cloud"
    - from: "frontend/nginx.conf upstream backend"
      to: "docker-compose.prod.yml api service"
      via: "container_name fb_testesfc-api resolvido na rede fb_net"
      pattern: "container_name: fb_testesfc-api"
---

<objective>
Configurar o deploy de produção do FB_TESTESFC no Coolify (Hostinger srv1306085, 76.13.171.196) para o domínio https://testesfc.fbtax.cloud.

Purpose: O projeto já existe no Coolify e o DNS já aponta para o servidor. Falta o compose de produção (com isolamento de rede correto para evitar colisão de DNS entre stacks) e o workflow de CI que dispara o redeploy automático a cada push em main.
Output: `docker-compose.prod.yml` (stack api+web+db, sem monitoramento) e `.github/workflows/deploy-production.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Referências para gerar os arquivos. NÃO alterar estes arquivos existentes. -->

docker-compose.yml (dev, serviços/env vars existentes — usar como base para o compose de prod):
- api: build context "." dockerfile "backend/Dockerfile", container_name fb_testesfc-api, PORT=8085,
  DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}?sslmode=disable,
  SMTP_HOST/PORT/USER/PASSWORD/FROM, APP_URL, JWT_SECRET, ENCRYPTION_KEY, COOKIE_SECURE, ALLOWED_ORIGINS
- web: build context "./frontend", container_name fb_testesfc-web
- db: postgres:15-alpine, container_name fb_testesfc-db, POSTGRES_USER/PASSWORD/DB de ${DB_USER}/${DB_PASSWORD}/${DB_NAME}, healthcheck pg_isready

frontend/nginx.conf: contém `upstream backend { server fb_testesfc-api:8085; }` HARDCODED.
=> O container da API em produção DEVE se chamar exatamente `fb_testesfc-api` e estar na mesma rede que o web (fb_net).

backend/Dockerfile: multi-stage, runtime = alpine:latest + ca-certificates (NÃO tem curl; busybox wget está disponível). EXPOSE 8085. Endpoint de health real: GET /api/health (registrado em backend/main.go:367).

Padrão de referência (OUTRO repo, só para copiar o formato — NÃO deste projeto):
/home/claudiobezerra/projetos/FB_APU04/docker-compose.prod.yml — mostra:
- networks de topo: `fb_net: { driver: bridge }` e `coolify: { external: true }`
- coolify conectado APENAS em web (e grafana); db/api/redis só em fb_net
- labels Traefik no serviço exposto (ver serviço grafana): traefik.enable, traefik.docker.network=coolify,
  routers.<nome>.rule=Host(...), entrypoints=https, tls=true, tls.certresolver=letsencrypt,
  services.<nome>.loadbalancer.server.port
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar docker-compose.prod.yml (stack api+web+db com isolamento de rede)</name>
  <files>docker-compose.prod.yml</files>
  <action>
Criar `docker-compose.prod.yml` na raiz do projeto com exatamente três serviços (api, web, db) — SEM stack de monitoramento (decisão do usuário). Copiar o padrão de rede e labels do arquivo de referência FB_APU04/docker-compose.prod.yml, adaptando nomes/portas/domínio para este projeto. NÃO criar Dockerfile.production separado — reusar backend/Dockerfile e frontend/Dockerfile existentes.

Serviço `api`:
- build: context "." e dockerfile "backend/Dockerfile"
- container_name: fb_testesfc-api  (MANTER EXATAMENTE ESTE NOME — o upstream em frontend/nginx.conf resolve `fb_testesfc-api:8085` hardcoded)
- restart: always
- SEM bloco `ports:` (produção não publica a porta da API no host). Usar apenas `expose: ["8085"]`
- environment: espelhar o serviço api do docker-compose.yml de dev, com estas mudanças de produção:
  - PORT=8085
  - DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}?sslmode=disable  (Postgres interno ao stack, sem TLS entre containers da mesma rede privada)
  - SMTP_HOST=${SMTP_HOST}, SMTP_PORT=${SMTP_PORT}, SMTP_USER=${SMTP_USER}, SMTP_PASSWORD=${SMTP_PASSWORD}, SMTP_FROM=${SMTP_FROM}
  - APP_URL=${APP_URL:-https://testesfc.fbtax.cloud}
  - JWT_SECRET=${JWT_SECRET}, ENCRYPTION_KEY=${ENCRYPTION_KEY}
  - COOKIE_SECURE=true  (fixo, NÃO ${COOKIE_SECURE:-false} — HTTPS obrigatório em produção)
  - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-}
- depends_on: db com condition service_healthy
- networks: apenas fb_net, com alias `testesfc-api` (defesa em profundidade; a api NÃO entra na rede coolify)
- healthcheck: usar busybox wget (curl NÃO existe na imagem alpine do backend). Test: wget -q --spider http://localhost:8085/api/health ; interval 30s, timeout 10s, retries 3, start_period 40s. O endpoint /api/health existe de fato (backend/main.go:367).

Serviço `web`:
- build: context "./frontend"
- container_name: fb_testesfc-web
- restart: always
- SEM `ports:`; usar `expose: ["80"]` (Traefik roteia via rede coolify, não publica no host)
- depends_on: api
- networks: fb_net E coolify
- labels Traefik (seguir o padrão do serviço grafana da referência, adaptado):
  - traefik.enable=true
  - traefik.docker.network=coolify
  - traefik.http.routers.testesfc.rule=Host(`testesfc.fbtax.cloud`)
  - traefik.http.routers.testesfc.entrypoints=https
  - traefik.http.routers.testesfc.tls=true
  - traefik.http.routers.testesfc.tls.certresolver=letsencrypt
  - traefik.http.services.testesfc.loadbalancer.server.port=80

Serviço `db`:
- image: postgres:15-alpine
- container_name: fb_testesfc-db
- restart: always
- environment: POSTGRES_USER=${DB_USER}, POSTGRES_PASSWORD=${DB_PASSWORD}, POSTGRES_DB=${DB_NAME}
- SEM `ports:` (produção não expõe Postgres ao host)
- volumes: postgres_data_testesfc_prod:/var/lib/postgresql/data
- networks: apenas fb_net (NUNCA coolify — regra de segurança crítica: db em rede pública compartilhada causaria colisão de DNS entre stacks no Traefik/Coolify)
- healthcheck: pg_isready -U ${DB_USER} -d ${DB_NAME} ; interval 10s, timeout 5s, retries 5, start_period 30s

Bloco `networks:` de topo:
- fb_net: { driver: bridge }
- coolify: { external: true }

Bloco `volumes:` de topo:
- postgres_data_testesfc_prod: { driver: local }
  </action>
  <verify>
    <automated>docker compose -f docker-compose.prod.yml config >/dev/null 2>&1 && echo VALID; grep -q "external: true" docker-compose.prod.yml && echo HAS_EXTERNAL; awk '/^  db:/{f=1} f&&/coolify/{print "DB_ON_COOLIFY_FAIL"; exit} /^  [a-z]/&&!/^  db:/{f=0}' docker-compose.prod.yml; grep -q "container_name: fb_testesfc-api" docker-compose.prod.yml && echo API_NAME_OK; grep -q "Host(\`testesfc.fbtax.cloud\`)" docker-compose.prod.yml && echo HOST_OK</automated>
  </verify>
  <done>
docker-compose.prod.yml existe, `docker compose config` valida sem erro, imprime VALID + HAS_EXTERNAL + API_NAME_OK + HOST_OK, e NÃO imprime DB_ON_COOLIFY_FAIL. web na rede coolify com labels Traefik; api e db só em fb_net; COOKIE_SECURE=true; nenhuma porta publicada ao host.
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar workflow de deploy que dispara o webhook do Coolify</name>
  <files>.github/workflows/deploy-production.yml</files>
  <action>
Criar `.github/workflows/deploy-production.yml` (o diretório .github/workflows/ ainda não existe — criar). Padrão simplificado: SEM job de build/push para GHCR (o Coolify builda do source, não puxa imagem — job de push é dead weight comprovado).

Conteúdo:
- name: Deploy to Production (Coolify)
- on: push nas branches [main] E workflow_dispatch (permite disparo manual, útil para branches não-main)
- Um único job `deploy-coolify`, runs-on: ubuntu-latest
- if do job: github.ref == 'refs/heads/main' (só dispara automaticamente em main; workflow_dispatch manual cobre outras branches)
- Um único step "Trigger Coolify deploy" com o comando curl EXATO abaixo (os flags de timeout/retry são deliberados — mitigam instabilidade conhecida de firewall na porta 8000 do Coolify; PRESERVAR exatamente):
    curl -fsSL -X GET "${{ secrets.COOLIFY_WEBHOOK_URL }}" -H "Authorization: Bearer ${{ secrets.COOLIFY_TOKEN }}" --connect-timeout 10 --max-time 30 --retry 2 --retry-delay 5

Usar os secrets COOLIFY_WEBHOOK_URL e COOLIFY_TOKEN via ${{ secrets.* }}. Não hardcodar URL nem token.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/deploy-production.yml')); assert 'deploy-coolify' in d['jobs']; print('YAML_OK')"; grep -q "COOLIFY_WEBHOOK_URL" .github/workflows/deploy-production.yml && echo WEBHOOK_OK; grep -q -- "--connect-timeout 10" .github/workflows/deploy-production.yml && grep -q -- "--retry 2" .github/workflows/deploy-production.yml && echo CURL_FLAGS_OK; grep -q "workflow_dispatch" .github/workflows/deploy-production.yml && echo DISPATCH_OK</automated>
  </verify>
  <done>
.github/workflows/deploy-production.yml existe e é YAML válido; imprime YAML_OK + WEBHOOK_OK + CURL_FLAGS_OK + DISPATCH_OK. Job único deploy-coolify, trigger push main + workflow_dispatch, curl com os flags de timeout/retry preservados, secrets referenciados (não hardcodados).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Traefik → web | Tráfego HTTPS externo entra apenas pelo serviço web via rede coolify |
| web (nginx) → api | Proxy interno na rede privada fb_net (api não exposta ao Traefik) |
| GitHub Actions → Coolify webhook | Disparo de redeploy autenticado por token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-k3u-01 | Spoofing/Information Disclosure | db na rede coolify | mitigate | db NUNCA na rede coolify — só fb_net; evita colisão de DNS entre stacks e exposição do Postgres ao Traefik (incidente real documentado) |
| T-k3u-02 | Information Disclosure | api exposta ao host/Traefik | mitigate | api sem `ports:` e fora da rede coolify; só `expose: 8085` em fb_net; acesso apenas via proxy nginx do web |
| T-k3u-03 | Information Disclosure | Postgres exposto ao host | mitigate | db sem `ports:` publicadas; acesso só interno ao stack |
| T-k3u-04 | Tampering/Elevation | Webhook de deploy | mitigate | COOLIFY_WEBHOOK_URL e COOLIFY_TOKEN via GitHub Secrets, nunca hardcodados; job só dispara em refs/heads/main |
| T-k3u-05 | Information Disclosure | Cookies de sessão sem TLS flag | mitigate | COOKIE_SECURE=true fixo em produção |
</threat_model>

<verification>
- `docker compose -f docker-compose.prod.yml config` valida sem erro
- db não aparece na rede coolify; api não aparece na rede coolify; apenas web em ambas fb_net + coolify
- container_name da api é exatamente fb_testesfc-api (compatível com upstream do nginx.conf)
- workflow YAML válido, dispara webhook Coolify com secrets e flags de timeout/retry preservados
</verification>

<success_criteria>
- docker-compose.prod.yml pronto para o Coolify buildar do source (api+web+db, sem monitoramento)
- Isolamento de rede correto: apenas web em coolify; db e api só em fb_net
- Traefik roteando testesfc.fbtax.cloud → web:80 com TLS letsencrypt
- Push em main dispara redeploy automático no Coolify
</success_criteria>

<output>
Create `.planning/quick/260702-k3u-configurar-deploy-de-produ-o-no-coolify-/260702-k3u-SUMMARY.md` when done
</output>
