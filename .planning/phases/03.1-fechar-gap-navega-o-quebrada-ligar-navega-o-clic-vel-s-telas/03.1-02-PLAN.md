---
phase: 03.1-fechar-gap-navegacao-quebrada
plan: 02
type: execute
wave: 2
depends_on: [03.1-01]
files_modified:
  - frontend/src/components/AppRail.tsx
  - frontend/src/App.tsx
  - frontend/src/pages/Login.tsx
autonomous: true
requirements: [XML-01, XML-02, XML-03, XML-04, ERP-02, ERP-03, FIS-01, FIS-02, FIS-03, CMP-01, CMP-02, CMP-03, CMP-04, AUTH-01, AUTH-02, TEN-02]

must_haves:
  truths:
    - "The AppRail shows 4 clickable module icons (Importações, Notas Importadas, Comparação Fiscal, Configurações), each navigating to its screen"
    - "Clicking the Configurações icon lands every role on a reachable page (/config/ambiente, a ProtectedRoute), never bouncing a non-admin to '/'"
    - "The Configurações tab bar renders inside the app layout via ModuleTabs"
    - "After login, and on the root route '/', all users are sent to /importacoes/comparacao-fiscal (a ProtectedRoute), so non-admin users no longer hit the '/' ↔ '/config/erp-bridge' redirect loop"
  artifacts:
    - path: "frontend/src/components/AppRail.tsx"
      provides: "4 clickable module icons + Configurações icon targeting a role-safe route"
      contains: "importacoes"
    - path: "frontend/src/App.tsx"
      provides: "ModuleTabs rendered in layout + root redirect to comparacao-fiscal"
      contains: "ModuleTabs"
    - path: "frontend/src/pages/Login.tsx"
      provides: "post-login redirect to comparacao-fiscal"
      contains: "/importacoes/comparacao-fiscal"
  key_links:
    - from: "frontend/src/components/AppRail.tsx"
      to: "route paths"
      via: "navigate(item.path) in mainItems loop"
      pattern: "navigate\\(item\\.path\\)"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/ModuleTabs.tsx"
      via: "import + <ModuleTabs /> render in AppLayout"
      pattern: "<ModuleTabs"
    - from: "frontend/src/pages/Login.tsx"
      to: "/importacoes/comparacao-fiscal"
      via: "navigate after login"
      pattern: "navigate\\(\"/importacoes/comparacao-fiscal\"\\)"
---

<objective>
Wire the navigation from plan 01 into the actual UI and fix the two broken redirects.

This plan closes the wiring half of **BLOCKER-1** (no clickable path to the business screens: `AppRail.tsx:34` has `mainItems = []` and `App.tsx:54` has a dead `// Sem ModuleTabs` comment) and all of **BLOCKER-2** (`Login.tsx:58` and the `/` route in `App.tsx:58` both redirect to the AdminRoute-only `/config/erp-bridge`, trapping non-admin users in an infinite `/` ↔ `/config/erp-bridge` loop). See `.planning/v1.0-MILESTONE-AUDIT.md`.

It also fixes the AppRail Configurações icon (flagged in PATTERNS.md): it currently navigates to `/config/erp-bridge` (AdminRoute), which bounces non-admin users to `/`. We retarget it to `/config/ambiente` (ProtectedRoute) so every role lands on a real Configurações page — the same class of role-safe-landing fix as D-07/D-08.

Purpose: Make every business screen reachable by clicking, for both admin and non-admin roles, with no redirect loop.
Output: Edited AppRail.tsx, App.tsx, Login.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/03.1-fechar-gap-navega-o-quebrada-ligar-navega-o-clic-vel-s-telas/03.1-CONTEXT.md
@.planning/phases/03.1-fechar-gap-navega-o-quebrada-ligar-navega-o-clic-vel-s-telas/03.1-PATTERNS.md

<interfaces>
<!-- Produced by plan 01 (wave 1). Consume, do not redefine. -->
From frontend/src/lib/navigation.ts:
- modules: Record<string, ModuleConfig> with keys 'importacoes' | 'notas' | 'comparacao' | 'config'
- getActiveModule(pathname): string → one of the 4 keys
From frontend/src/components/ModuleTabs.tsx:
- export function ModuleTabs(): JSX.Element | null  (self-contained: no props; uses useLocation + getActiveModule + role internally)

AppRail item id values MUST equal the module keys used by getActiveModule so the active-state highlight matches: 'importacoes', 'notas', 'comparacao'. (The existing hardcoded Config icon keeps its `active === 'config'` check.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate AppRail mainItems (4 clickable module icons) + role-safe Config icon</name>
  <files>frontend/src/components/AppRail.tsx</files>
  <read_first>
    - frontend/src/components/AppRail.tsx (current full file — especially import line 1, mainItems line 33-34, the map loop lines 96-118, the Config icon button lines 121-138)
    - .planning/phases/03.1-fechar-gap-navega-o-quebrada-ligar-navega-o-clic-vel-s-telas/03.1-PATTERNS.md (AppRail.tsx section, lines 95-185 — includes the flagged Config-icon concern at line 180)
    - frontend/src/lib/navigation.ts (module keys, so ids match)
  </read_first>
  <action>
    Edit frontend/src/components/AppRail.tsx.
    1. Add the three module icons to the existing `lucide-react` import (line 1, currently `import { Settings, LogOut, KeyRound } from 'lucide-react'`): add `Upload`, `ListChecks`, `Scale` (all confirmed present in lucide-react).
    2. Replace the empty mainItems declaration (line 33-34, the `const mainItems ... = []` preceded by the "Fase 1: nenhum módulo de negócio" comment) with an array of exactly 3 entries. Each entry's `id` MUST match a module key from navigation.ts (do NOT use 'config' here — the Configurações icon is a separate hardcoded block below): { id: 'importacoes', icon: Upload, label: 'Importações', path: '/importacoes/xmls-saida' }, { id: 'notas', icon: ListChecks, label: 'Notas Importadas', path: '/importacoes/notas-saida' }, { id: 'comparacao', icon: Scale, label: 'Comparação Fiscal', path: '/importacoes/comparacao-fiscal' }. The existing `mainItems.map(...)` loop (lines 96-118) with its Tooltip + `active === item.id` styling renders these automatically — do not change the loop structure.
    3. Fix the hardcoded Configurações icon button (its onClick is `navigate('/config/erp-bridge')` at line 126): change the target to `navigate('/config/ambiente')`. Reason: `/config/erp-bridge` is AdminRoute-only, so a non-admin clicking Configurações is bounced to `/` (a dead end); `/config/ambiente` is a ProtectedRoute reachable by all roles and is the first non-adminOnly tab of the config module. Leave the `active === 'config'` class check, the `Settings` icon, and the tooltip text 'Configurações' unchanged.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -Eq "id: 'importacoes'" src/components/AppRail.tsx && grep -Eq "id: 'notas'" src/components/AppRail.tsx && grep -Eq "id: 'comparacao'" src/components/AppRail.tsx && grep -q "navigate('/config/ambiente')" src/components/AppRail.tsx && ! grep -q "navigate('/config/erp-bridge')" src/components/AppRail.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` in frontend/ exits 0.
    - mainItems has exactly 3 entries with ids importacoes / notas / comparacao (source assertion).
    - Behavior: clicking the Importações icon navigates to /importacoes/xmls-saida; Notas Importadas → /importacoes/notas-saida; Comparação Fiscal → /importacoes/comparacao-fiscal (each observable in the running app; the active icon highlights via getActiveModule).
    - Behavior: clicking the Configurações icon navigates to /config/ambiente and renders the Ambiente page for BOTH admin and non-admin (no bounce to '/'). Source assertion: `navigate('/config/erp-bridge')` no longer appears in AppRail.tsx; `navigate('/config/ambiente')` does.
  </acceptance_criteria>
  <done>AppRail shows 4 working module icons; Configurações lands every role on /config/ambiente.</done>
</task>

<task type="auto">
  <name>Task 2: Render ModuleTabs in the layout + fix root & post-login redirects</name>
  <files>frontend/src/App.tsx, frontend/src/pages/Login.tsx</files>
  <read_first>
    - frontend/src/App.tsx (current file — import block lines 1-18, AppLayout header/main lines 45-69 with the dead comment at line 54, routes lines 57-67, ProtectedRoute/AdminRoute wrappers lines 22-37)
    - frontend/src/pages/Login.tsx (handleLogin lines 38-66, the navigate call at line 58, the useNavigate import at line 7)
    - .planning/phases/03.1-fechar-gap-navega-o-quebrada-ligar-navega-o-clic-vel-s-telas/03.1-PATTERNS.md (App.tsx section lines 207-242, Login.tsx section lines 189-203)
    - frontend/src/components/ModuleTabs.tsx (the component to import — confirm export name and that it takes no props)
  </read_first>
  <action>
    Edit two files.
    frontend/src/App.tsx:
    1. Add `import { ModuleTabs } from '@/components/ModuleTabs'` to the import block (alongside the AppRail import).
    2. Replace the dead comment `{/* Sem ModuleTabs */}` at line 54 (inside AppLayout, between the header block lines 45-53 and the main block lines 55-69) with `<ModuleTabs />`. No props — ModuleTabs internally uses useLocation + getActiveModule and returns null for single-tab modules, so this single line is the only change to AppLayout.
    3. Change the root route (line 58) from `<Navigate to="/config/erp-bridge" replace />` to `<Navigate to="/importacoes/comparacao-fiscal" replace />` (D-08). Do NOT touch any other Route, nor the ProtectedRoute/AdminRoute wrappers.
    frontend/src/pages/Login.tsx:
    4. Change the post-login redirect at line 58 from `navigate("/config/erp-bridge")` to `navigate("/importacoes/comparacao-fiscal")` (D-07). useNavigate is already imported; make no other change.
    Both redirect targets are ProtectedRoute (not AdminRoute), which is what breaks the non-admin redirect loop (BLOCKER-2).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build >/dev/null 2>&1 && grep -q "<ModuleTabs" src/App.tsx && ! grep -q "Sem ModuleTabs" src/App.tsx && grep -q 'Navigate to="/importacoes/comparacao-fiscal"' src/App.tsx && ! grep -q 'Navigate to="/config/erp-bridge"' src/App.tsx && grep -q 'navigate("/importacoes/comparacao-fiscal")' src/pages/Login.tsx && ! grep -q 'navigate("/config/erp-bridge")' src/pages/Login.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds.
    - App.tsx renders `<ModuleTabs />` and the string "Sem ModuleTabs" is gone (source assertion). Behavior: navigating to /config/ambiente shows the Configurações tab bar above the page content; navigating to /importacoes/notas-saida or /importacoes/comparacao-fiscal shows no bar.
    - App.tsx root route and Login.tsx both point to /importacoes/comparacao-fiscal; neither points to /config/erp-bridge anymore (source assertion).
    - Behavior: a non-admin (role='user') logging in lands on /importacoes/comparacao-fiscal with the Comparação Fiscal screen rendered — no infinite redirect, no blank page.
  </acceptance_criteria>
  <done>ModuleTabs renders in the layout; root and login redirects target the ProtectedRoute comparacao-fiscal page, ending the non-admin loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → route access | Client redirects and nav clicks choose destinations; server + AdminRoute wrappers (unchanged) enforce actual access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-03 | Elevation of Privilege | new post-login / root redirect target | mitigate | Both redirects target /importacoes/comparacao-fiscal, a ProtectedRoute (authenticated, any role) — not an AdminRoute. No code path grants a non-admin access to an admin route; the AdminRoute wrappers on /config/erp-bridge, /importacoes/erp-bridge, /config/usuarios are untouched and still redirect non-admins away. |
| T-03.1-04 | Elevation of Privilege | AppRail Configurações icon target | mitigate | Retarget from AdminRoute (/config/erp-bridge) to ProtectedRoute (/config/ambiente). This does not expand any user's access — /config/ambiente was already reachable by all authenticated users; it just removes a dead-end bounce. Admin-only config pages remain reachable only via their adminOnly tabs (filtered by ModuleTabs, plan 01) behind AdminRoute wrappers. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — Upload/ListChecks/Scale from existing lucide-react; ModuleTabs from this repo. No supply-chain surface added. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npm run build` succeed.
- grep confirms: AppRail has 3 module ids + Config icon → /config/ambiente; App.tsx renders <ModuleTabs/> and root → comparacao-fiscal; Login → comparacao-fiscal; no remaining reference to the old /config/erp-bridge redirect in these two navigation paths.
- Full clickable behavior (admin + non-admin) is proven by the human checkpoint in plan 03.
</verification>

<success_criteria>
- Every business screen is reachable by clicking an AppRail icon (BLOCKER-1 closed at the UI layer).
- Non-admin users log in and land on a rendered page with no redirect loop; the Configurações icon lands every role on a real page (BLOCKER-2 + flagged Config-icon concern closed).
- Type-check and build clean.
</success_criteria>

<output>
Create `.planning/phases/03.1-fechar-gap-navega-o-quebrada-ligar-navega-o-clic-vel-s-telas/03.1-02-SUMMARY.md` when done.
</output>
