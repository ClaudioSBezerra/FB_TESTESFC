---
phase: 03.1-fechar-gap-navegacao-quebrada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/navigation.ts
  - frontend/src/components/ModuleTabs.tsx
autonomous: true
requirements: [XML-01, XML-02, XML-03, XML-04, ERP-02, ERP-03, FIS-01, FIS-02, FIS-03, CMP-01, CMP-02, CMP-03, CMP-04]

must_haves:
  truths:
    - "getActiveModule returns one of 4 module keys (importacoes/notas/comparacao/config) instead of always 'config'"
    - "The 3 business screens (Importar XMLs, Notas Importadas, Comparação Fiscal) each live under their own module key, no longer under config.tabs"
    - "A ModuleTabs component exists that renders a horizontal tab bar only for multi-tab modules and hides adminOnly tabs from non-admin users"
  artifacts:
    - path: "frontend/src/lib/navigation.ts"
      provides: "modules Record with 4 keys + getActiveModule mapping 4 path prefixes"
      contains: "importacoes"
    - path: "frontend/src/components/ModuleTabs.tsx"
      provides: "Router-driven horizontal tab bar with adminOnly filtering"
      exports: ["ModuleTabs"]
  key_links:
    - from: "frontend/src/components/ModuleTabs.tsx"
      to: "frontend/src/lib/navigation.ts"
      via: "import { modules, getActiveModule }"
      pattern: "getActiveModule|modules\\["
    - from: "frontend/src/components/ModuleTabs.tsx"
      to: "user.role"
      via: "adminOnly filter using useAuth"
      pattern: "adminOnly.*role|role.*admin"
---

<objective>
Build the navigation data model and the tab-bar component that BLOCKER-1 needs.

This plan closes the data-layer half of **BLOCKER-1** (`.planning/v1.0-MILESTONE-AUDIT.md`): the app defines business screens in `navigation.ts` but the module map only ever resolves to `'config'` and there is no component that renders a per-module tab bar. Here we rewrite `navigation.ts` into 4 real modules and create the missing `ModuleTabs` component (referenced by a dead comment at `App.tsx:54` but never implemented). The clickable AppRail icons and the wiring into the layout come in plan 02 (wave 2), which imports this plan's output.

Purpose: Give the UI a correct, role-aware source of truth for module → pages → active-state, so plan 02 can wire clickable navigation on top of it.
Output: Rewritten `navigation.ts` (4 modules) and new `ModuleTabs.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/03.1-fechar-gap-navega-o-quebrada-ligar-navega-o-clic-vel-s-telas/03.1-CONTEXT.md
@.planning/phases/03.1-fechar-gap-navega-o-quebrada-ligar-navega-o-clic-vel-s-telas/03.1-PATTERNS.md

<interfaces>
<!-- Contracts consumed by ModuleTabs and (in plan 02) AppRail/App. Do NOT modify the two interfaces below. -->

From frontend/src/lib/navigation.ts (interfaces are correct as-is, keep verbatim):
- interface ModuleTab { label: string; path: string; disabled?: boolean; danger?: boolean; adminOnly?: boolean }
- interface ModuleConfig { label: string; tabs: ModuleTab[] }

Role-check idiom (the ONLY one in the codebase — reuse verbatim, do not invent a helper):
- From frontend/src/App.tsx AdminRoute (line 35): `user?.role !== 'admin'`
- user.role comes from useAuth() in frontend/src/contexts/AuthContext.tsx (User.role?: string)

Route paths that MUST match verbatim (from frontend/src/App.tsx routes, lines 57-66):
- /importacoes/xmls-saida  (ProtectedRoute → ImportarXMLsSaida)
- /importacoes/notas-saida (ProtectedRoute → ConsultaNFeSaidas)
- /importacoes/comparacao-fiscal (ProtectedRoute → ComparacaoFiscal)
- /importacoes/erp-bridge (AdminRoute → ERPBridgeConfig)
- /config/erp-bridge (AdminRoute → ERPBridgeCredenciais)
- /config/ambiente (ProtectedRoute → GestaoAmbiente)
- /config/gestores (ProtectedRoute → Managers)
- /config/usuarios (AdminRoute → AdminUsers)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite navigation.ts into 4 modules + real getActiveModule</name>
  <files>frontend/src/lib/navigation.ts</files>
  <read_first>
    - frontend/src/lib/navigation.ts (current full 34-line content — read before editing)
    - .planning/phases/03.1-fechar-gap-navega-o-quebrada-ligar-navega-o-clic-vel-s-telas/03.1-PATTERNS.md (navigation.ts section, lines 21-91)
    - frontend/src/App.tsx (routes block lines 57-66, to confirm path strings verbatim)
  </read_first>
  <action>
    Edit frontend/src/lib/navigation.ts. Do NOT touch the `ModuleTab` and `ModuleConfig` interfaces (lines 1-12) — they are already correct.

    Replace the `modules` object (currently a single `config` key) with a `Record<string, ModuleConfig>` holding exactly 4 keys in this order: `importacoes`, `notas`, `comparacao`, `config`.
    - `importacoes` — label 'Importações', tabs: single entry { label: 'Importar XMLs', path: '/importacoes/xmls-saida' } (D-03: modeled as a module for future pages, one page today).
    - `notas` — label 'Notas Importadas', tabs: single entry { label: 'Notas Importadas', path: '/importacoes/notas-saida' } (D-02).
    - `comparacao` — label 'Comparação Fiscal', tabs: single entry { label: 'Comparação Fiscal', path: '/importacoes/comparacao-fiscal' } (D-02).
    - `config` — label 'Configurações', keep EXACTLY these 5 tabs verbatim, same order, same labels, same adminOnly flags (D-04): 'Credenciais ERP' /config/erp-bridge adminOnly:true; 'Config ERP' /importacoes/erp-bridge adminOnly:true; 'Ambiente' /config/ambiente; 'Gestores' /config/gestores; 'Usuários' /config/usuarios adminOnly:true.
    The three business tabs (Importar XMLs, Notas Importadas, Comparação Fiscal) MOVE OUT of config.tabs into their own module keys — they must no longer appear under config.

    Rewrite `getActiveModule(pathname)` to map real path prefixes instead of always returning 'config': startsWith('/importacoes/xmls-saida') → 'importacoes'; startsWith('/importacoes/notas-saida') → 'notas'; startsWith('/importacoes/comparacao-fiscal') → 'comparacao'; startsWith('/config/') OR startsWith('/importacoes/erp-bridge') → 'config'; fallback return 'comparacao' (matches the new default landing page per D-07/D-08). Order the three specific /importacoes checks before the /importacoes/erp-bridge check so the ERP config page resolves to 'config'.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -Eq "importacoes:|notas:|comparacao:|config:" src/lib/navigation.ts && grep -Eq "return 'importacoes'" src/lib/navigation.ts && grep -Eq "return 'notas'" src/lib/navigation.ts && grep -Eq "return 'comparacao'" src/lib/navigation.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` in frontend/ exits 0.
    - `modules` is a `Record<string, ModuleConfig>` with exactly 4 keys: importacoes, notas, comparacao, config (source assertion).
    - config.tabs contains exactly the 5 config/admin entries and does NOT contain any of '/importacoes/xmls-saida', '/importacoes/notas-saida', '/importacoes/comparacao-fiscal' (source assertion).
    - getActiveModule('/importacoes/xmls-saida') resolves to 'importacoes', '/importacoes/notas-saida' to 'notas', '/importacoes/comparacao-fiscal' to 'comparacao', '/config/ambiente' to 'config', '/importacoes/erp-bridge' to 'config', and an unknown path returns 'comparacao' (behavior assertion — readable from the branch structure).
  </acceptance_criteria>
  <done>navigation.ts exposes 4 modules and a getActiveModule that maps the 4 real path groups; type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create ModuleTabs.tsx (router-driven, adminOnly-filtered tab bar)</name>
  <files>frontend/src/components/ModuleTabs.tsx</files>
  <read_first>
    - .planning/phases/03.1-fechar-gap-navega-o-quebrada-ligar-navega-o-clic-vel-s-telas/03.1-PATTERNS.md (ModuleTabs.tsx section, lines 246-303 — the recommended implementation and role-check source)
    - frontend/src/components/AppRail.tsx (lines 96-118 — the button + cn active-state idiom to mirror)
    - frontend/src/lib/navigation.ts (just edited — the modules/getActiveModule contract)
    - frontend/src/contexts/AuthContext.tsx (User.role field, useAuth signature)
  </read_first>
  <action>
    Create new file frontend/src/components/ModuleTabs.tsx exporting a named function component `ModuleTabs`.
    Imports: useLocation, useNavigate from 'react-router-dom'; cn from '@/lib/utils'; useAuth from '@/contexts/AuthContext'; modules and getActiveModule from '@/lib/navigation'.
    Logic: const active = getActiveModule(location.pathname); const config = modules[active]. Return null when `!config || config.tabs.length <= 1` — this implements D-05, so single-page modules (Notas Importadas, Comparação Fiscal, and currently Importações with its one tab) render no bar; only Configurações (5 tabs) shows a bar today.
    Render: a horizontal container using classes `flex items-center gap-1 h-10 border-b bg-white px-4 shrink-0`. Map config.tabs; for each tab, skip it with `if (tab.adminOnly && user?.role !== 'admin') return null` (D-06 — this exact idiom matches App.tsx AdminRoute line 35; do not invent a new role helper). isActive = location.pathname.startsWith(tab.path); onClick = () => navigate(tab.path); button className via cn with base `px-3 py-1.5 text-sm font-medium rounded-md transition-colors` and active state `bg-primary/10 text-primary` vs `text-muted-foreground hover:bg-gray-100 hover:text-foreground`. key={tab.path}, render tab.label as content.
    Do NOT use the Radix Tabs/TabsList/TabsTrigger primitives from ui/tabs.tsx — this is router navigation between distinct routes, not in-page content switching. Use plain buttons + navigate, following the AppRail idiom.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "export function ModuleTabs" src/components/ModuleTabs.tsx && grep -Eq "tabs\.length <= 1" src/components/ModuleTabs.tsx && grep -Eq "adminOnly && user\?\.role !== 'admin'" src/components/ModuleTabs.tsx && ! grep -Eq "TabsList|TabsTrigger" src/components/ModuleTabs.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` in frontend/ exits 0.
    - ModuleTabs returns null for a module whose tabs.length <= 1 (source assertion: the guard exists) — so navigating to /importacoes/notas-saida or /importacoes/comparacao-fiscal renders NO tab bar (behavior).
    - For the config module, an adminOnly tab is skipped when user.role !== 'admin' (source assertion of the filter) — behavior: a non-admin viewing /config/ambiente sees only Ambiente and Gestores in the bar, never Credenciais ERP / Config ERP / Usuários.
    - The file does NOT import or use TabsList/TabsTrigger (source assertion).
  </acceptance_criteria>
  <done>ModuleTabs.tsx exists, type-checks, self-hides for single-tab modules, and filters adminOnly tabs by role.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → route access | The React client decides which nav items to show based on `user.role`. This is UX only; the authoritative gate is the server + the AdminRoute wrapper in App.tsx (unchanged this phase). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Elevation of Privilege | ModuleTabs rendering config.tabs | mitigate | Filter `tab.adminOnly && user?.role !== 'admin'` before rendering each tab, so admin-only routes are not surfaced as clickable links to non-admins. Defense-in-depth: the routes themselves remain AdminRoute-wrapped in App.tsx, which is the real enforcement — hiding the tab is UX, not the security boundary. |
| T-03.1-02 | Information Disclosure | admin tab labels visible to non-admin | mitigate | Same adminOnly filter removes the labels entirely; non-admins cannot see that Credenciais ERP / Config ERP / Usuários exist. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | No new package installs this phase — Upload/ListChecks/Scale come from the existing `lucide-react` dependency; react-router-dom, cn, useAuth all pre-existing. No supply-chain surface added. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0 after both tasks.
- `grep` confirms 4 module keys in navigation.ts and the adminOnly filter + single-tab guard in ModuleTabs.tsx.
- Behavioral proof (clickable navigation, role filtering) is covered by the human checkpoint in plan 03 — this plan only produces the data model + component.
</verification>

<success_criteria>
- navigation.ts exposes 4 modules and a getActiveModule that resolves the 4 real path groups (not always 'config').
- ModuleTabs.tsx exists, is router-driven, self-hides for single-tab modules, and hides adminOnly tabs from non-admin users.
- Type-check clean; no Radix Tabs primitives used.
</success_criteria>

<output>
Create `.planning/phases/03.1-fechar-gap-navega-o-quebrada-ligar-navega-o-clic-vel-s-telas/03.1-01-SUMMARY.md` when done.
</output>
