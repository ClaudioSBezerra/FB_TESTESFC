---
phase: 03.1-fechar-gap-navegacao-quebrada
plan: 01
subsystem: ui
tags: [react, react-router, navigation, typescript]

# Dependency graph
requires:
  - phase: 03
    provides: ComparacaoFiscal page and ProtectedRoute wiring in App.tsx that this plan's fallback module targets
provides:
  - navigation.ts rewritten into 4 real modules (importacoes, notas, comparacao, config) with a getActiveModule that maps real path prefixes
  - ModuleTabs.tsx component: router-driven horizontal tab bar, self-hides for single-tab modules, filters adminOnly tabs by role
affects: [03.1-02, 03.1-03]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Router-driven tab bar (ModuleTabs) using useLocation/useNavigate + cn() active-state idiom, not Radix Tabs primitives, for navigation between distinct routes"
    - "adminOnly tab filtering with the existing `user?.role !== 'admin'` idiom (same as AdminRoute in App.tsx), first render-time consumer of ModuleTab.adminOnly"

key-files:
  created: [frontend/src/components/ModuleTabs.tsx]
  modified: [frontend/src/lib/navigation.ts]

key-decisions:
  - "config.tabs kept to exactly the 5 admin/config entries; the 3 business tabs (Importar XMLs, Notas Importadas, Comparação Fiscal) moved into their own module keys (D-01/D-02/D-03 from 03.1-CONTEXT.md)"
  - "getActiveModule fallback returns 'comparacao' (not 'config') to match the new post-login/root landing page decided in D-07/D-08, to be wired in plan 02"

patterns-established:
  - "ModuleTabs self-contains its own useLocation + getActiveModule lookup, mirroring how AppRail already self-contains this logic — keeps App.tsx changes in plan 02 to a single `<ModuleTabs />` line"

requirements-completed: [XML-01, XML-02, XML-03, XML-04, ERP-02, ERP-03, FIS-01, FIS-02, FIS-03, CMP-01, CMP-02, CMP-03, CMP-04]

# Metrics
duration: 8min
completed: 2026-07-02
---

# Phase 03.1 Plan 01: Navigation data model + ModuleTabs component Summary

**Rewrote navigation.ts into 4 real modules with a working getActiveModule, and created the missing ModuleTabs router-driven tab-bar component referenced by a dead comment in App.tsx.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-07-02T16:42:48Z
- **Completed:** 2026-07-02T16:50:00Z
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments
- `modules` in navigation.ts is now a `Record<string, ModuleConfig>` with 4 keys (`importacoes`, `notas`, `comparacao`, `config`) instead of a single `config` key holding all 8 pages
- `getActiveModule(pathname)` maps the 4 real path prefixes instead of always returning `'config'`, with fallback to `'comparacao'` matching the new landing page decided for plan 02
- New `ModuleTabs.tsx` renders a horizontal tab bar for multi-tab modules only (`config.tabs.length <= 1` guard), and hides `adminOnly` tabs from non-admin users using the codebase's existing role-check idiom

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite navigation.ts into 4 modules + real getActiveModule** - `0e0eec3` (feat)
2. **Task 2: Create ModuleTabs.tsx (router-driven, adminOnly-filtered tab bar)** - `d72e615` (feat)

**Plan metadata:** (pending — added by final metadata commit)

## Files Created/Modified
- `frontend/src/lib/navigation.ts` - 4-module `Record<string, ModuleConfig>` + real `getActiveModule` path-prefix mapping
- `frontend/src/components/ModuleTabs.tsx` - new component: router-driven tab bar, self-hides for single-tab modules, filters adminOnly tabs

## Decisions Made
- Followed plan verbatim: kept `ModuleTab`/`ModuleConfig` interfaces untouched, moved the 3 business tabs out of `config.tabs`, reused the `user?.role !== 'admin'` idiom rather than inventing a new role helper, and avoided Radix Tabs primitives in favor of plain buttons + `navigate()`.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `navigation.ts` and `ModuleTabs.tsx` are ready for plan 02, which wires the 4 AppRail icons and renders `<ModuleTabs />` in `App.tsx` in place of the dead `{/* Sem ModuleTabs */}` comment.
- Behavioral proof (clickable navigation, role filtering in the browser) is deferred to the human checkpoint in plan 03, per this plan's `<verification>` section — this plan only produced the data model + component, not the wiring.

---
*Phase: 03.1-fechar-gap-navegacao-quebrada*
*Completed: 2026-07-02*

## Self-Check: PASSED

- FOUND: frontend/src/lib/navigation.ts
- FOUND: frontend/src/components/ModuleTabs.tsx
- FOUND: .planning/phases/03.1-fechar-gap-navega-o-quebrada-ligar-navega-o-clic-vel-s-telas/03.1-01-SUMMARY.md
- FOUND: 0e0eec3 (Task 1 commit)
- FOUND: d72e615 (Task 2 commit)
