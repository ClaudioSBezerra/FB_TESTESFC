---
phase: 03.1-fechar-gap-navegacao-quebrada
plan: 03
type: execute
wave: 3
depends_on: [03.1-01, 03.1-02]
files_modified: []
autonomous: false
requirements: [XML-01, XML-02, XML-03, XML-04, ERP-02, ERP-03, FIS-01, FIS-02, FIS-03, CMP-01, CMP-02, CMP-03, CMP-04, AUTH-01, AUTH-02, TEN-02]

must_haves:
  truths:
    - "A real user (admin) can click from login all the way to every business screen with no manual URL typing"
    - "A real non-admin user (role='user') can log in and use the app with no redirect loop and no blank page"
    - "adminOnly config tabs are invisible to non-admin users but present for admin users"
  artifacts: []
  key_links:
    - from: "human tester"
      to: "every business screen"
      via: "clicking only — no URL bar"
      pattern: "manual click-through"
---

<objective>
Prove BLOCKER-1 and BLOCKER-2 are actually closed by clicking through the running app as BOTH an admin and a non-admin user — the exact verification that all prior phase checkpoints skipped (they navigated by typing URLs, which is why the navigation gap went undetected).

This plan closes **BLOCKER-1** and **BLOCKER-2** at the acceptance level (`.planning/v1.0-MILESTONE-AUDIT.md`). It has no code changes; its purpose is the click-path proof that the milestone audit demanded. No automated E2E runner (Playwright/vitest) exists in this project, so the click-through is a blocking human checkpoint after an automated build/type gate.

Purpose: Confirm the product is usable by clicking, for every role, before the v1.0 milestone can close.
Output: A passing human verification (admin + non-admin) recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/03.1-fechar-gap-navega-o-quebrada-ligar-navega-o-clic-vel-s-telas/03.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated build + type + lint gate before human click-through</name>
  <files>frontend/package.json (read-only gate; no files modified)</files>
  <read_first>
    - frontend/package.json (scripts: build, lint)
    - frontend/src/App.tsx, frontend/src/components/AppRail.tsx, frontend/src/components/ModuleTabs.tsx, frontend/src/lib/navigation.ts, frontend/src/pages/Login.tsx (confirm plans 01-02 landed before asking a human to test)
  </read_first>
  <action>
    Run the frontend verification gate so the human is only asked to test a build that compiles cleanly. From frontend/: run `npx tsc --noEmit`, then `npm run build`, then `npm run lint`. All must pass. If lint reports only pre-existing warnings unrelated to the 5 touched files, note them but do not fail — the gate fails only on type errors, build failures, or lint errors introduced in AppRail.tsx / App.tsx / Login.tsx / navigation.ts / ModuleTabs.tsx.
    Then start the dev server (`npm run dev`) so the human checkpoint can exercise it, and report the local URL (e.g. http://localhost:5173) in the checkpoint.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build >/dev/null 2>&1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm run build` completes successfully (bundle produced).
    - The dev server starts and serves the app at a reported local URL.
    - No lint error is introduced in the 5 files touched by plans 01-02.
  </acceptance_criteria>
  <done>Frontend type-checks, builds, and is running locally, ready for click-through verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human click-through verification (admin + non-admin)</name>
  <files>none — manual verification of the running app</files>
  <action>Pause execution and present the click-through steps below to the user. Do not proceed or mark the phase complete until the user responds with the resume signal.</action>
  <what-built>
    Clickable navigation for all business screens (4 AppRail module icons + a Configurações tab bar via the new ModuleTabs) and fixed post-login / root redirects. This is the fix for BLOCKER-1 (no clickable path to Importar XMLs / Notas Importadas / Comparação Fiscal) and BLOCKER-2 (non-admin redirect loop) from the v1.0 milestone audit.
  </what-built>
  <how-to-verify>
    Open the app at the local URL reported by Task 1 (e.g. http://localhost:5173). Do NOT type any URL into the address bar after login — navigate ONLY by clicking, since that is exactly what the audit found broken.

    PART A — as an ADMIN user:
    1. Log in with an admin account. Expected: you land on the Comparação Fiscal screen at /importacoes/comparacao-fiscal (NOT a blank page, NOT /config/erp-bridge, no flicker/loop).
    2. Confirm the left AppRail shows 4 module icons with tooltips on hover: Importações (upload icon), Notas Importadas (list icon), Comparação Fiscal (scale icon), and Configurações (gear icon).
    3. Click Importações → lands on Importar XMLs (/importacoes/xmls-saida). (No tab bar appears — Importações has a single page today; this is expected, not a bug.)
    4. Click Notas Importadas → lands on the Notas Importadas list (/importacoes/notas-saida). No tab bar (single page). Confirm the "Executar cálculo fiscal" action is present here.
    5. Click Comparação Fiscal → lands on the comparison screen (/importacoes/comparacao-fiscal). No tab bar (single page).
    6. Click Configurações → lands on Ambiente (/config/ambiente) AND a horizontal tab bar appears at the top of the content showing 5 tabs: Credenciais ERP, Config ERP, Ambiente, Gestores, Usuários. Click each tab and confirm it navigates to its page and highlights as active.
    7. Confirm the active AppRail icon highlights to match the section you are in.

    PART B — as a NON-ADMIN user (role='user'):
    8. If no non-admin account exists, create one via Configurações → Usuários (AdminUsers) as admin, then log out.
    9. Log in as the non-admin user. Expected (this is BLOCKER-2): you land on the Comparação Fiscal screen at /importacoes/comparacao-fiscal — NO infinite redirect, NO blank page, NO bounce between '/' and '/config/erp-bridge'.
    10. Click Configurações → lands on Ambiente (/config/ambiente), NOT bounced back to the landing page. The tab bar shows ONLY Ambiente and Gestores. Confirm the admin-only tabs (Credenciais ERP, Config ERP, Usuários) are NOT visible and NOT clickable (this is the adminOnly filter / privilege check — a non-admin must not see or reach admin config).
    11. Click Importações, Notas Importadas, and Comparação Fiscal — all reachable and render normally for the non-admin.

    Report PASS only if: every business screen is reachable by clicking for the admin; the non-admin logs in without a loop and reaches all three business screens; and admin-only tabs are hidden from the non-admin.
  </how-to-verify>
  <verify>
    <human-check>Admin (Part A, steps 1-7) and non-admin (Part B, steps 8-11) click-through both pass, including the adminOnly-tab hiding check at step 10.</human-check>
  </verify>
  <resume-signal>Type "approved" if all steps pass as described, or list the specific step number(s) and observed behavior that failed.</resume-signal>
  <done>User replied "approved" for BOTH the admin and non-admin click-through flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-admin client → admin config | A non-admin session must not be able to see or reach admin-only config pages via the new navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-05 | Elevation of Privilege | non-admin navigation surface | mitigate | Step 10 of the human checkpoint explicitly verifies a non-admin sees only Ambiente + Gestores in the config tab bar and that Credenciais ERP / Config ERP / Usuários are absent and unreachable — confirming the adminOnly filter (plan 01) holds at runtime and no privilege escalation was introduced by the new redirect/nav wiring. AdminRoute wrappers remain the enforcing gate. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (verification only). |
</threat_model>

<verification>
- Task 1 automated gate (tsc + build) passes.
- The blocking human checkpoint returns "approved" for BOTH admin (Part A) and non-admin (Part B) flows, including the adminOnly-tab hiding check (step 10).
</verification>

<success_criteria>
- BLOCKER-1 closed: every business screen (Importar XMLs, Notas Importadas, Comparação Fiscal) reached by clicking only, as admin.
- BLOCKER-2 closed: non-admin logs in and uses the app with no redirect loop, and admin-only config is hidden from them.
- Human verification recorded as approved in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/03.1-fechar-gap-navega-o-quebrada-ligar-navega-o-clic-vel-s-telas/03.1-03-SUMMARY.md` when done, recording the admin + non-admin click-through result.
</output>
