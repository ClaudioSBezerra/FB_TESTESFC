---
phase: 01-foundation-inherited-stack
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/handlers/admin.go
  - backend/handlers/environment.go
  - backend/handlers/hierarchy.go
  - backend/handlers/managers.go
  - backend/migrations/003_managers.sql
  - backend/main.go
autonomous: true
requirements: [TEN-02, TEN-03]
must_haves:
  truths:
    - "Admin lista, cria, promove, deleta e reatribui usuários via /api/admin/users*"
    - "Contexto de empresa (Ferreira Costa) é resolvido por GetEffectiveCompanyID em handlers por empresa"
    - "Hierarquia ambiente/grupo/empresa é consultável e editável via /api/config/*"
    - "Tabela managers existe após migração 003"
  artifacts:
    - path: "backend/handlers/admin.go"
      provides: "CRUD de usuários (List/Create/Promote/Delete/Reassign)"
      contains: "func ListUsersHandler"
    - path: "backend/handlers/hierarchy.go"
      provides: "GetUserHierarchyHandler (ambiente→grupo→empresa)"
      contains: "GetUserHierarchyHandler"
    - path: "backend/migrations/003_managers.sql"
      provides: "Tabela managers"
      contains: "managers"
  key_links:
    - from: "backend/main.go"
      to: "handlers.ListUsersHandler"
      via: "withAuth /api/admin/users role=admin"
      pattern: "/api/admin/users"
    - from: "backend/handlers/managers.go"
      to: "GetEffectiveCompanyID"
      via: "X-Company-ID → companyID"
      pattern: "GetEffectiveCompanyID"
---

<objective>
Adicionar a camada de gestão (tenancy) ao backend: handlers de administração de usuários, hierarquia ambiente/grupo/empresa, environment CRUD e managers, mais a migração 003 (tabela managers), e registrar suas rotas no `main.go`. Habilita o admin a gerenciar usuários e resolve o contexto de empresa Ferreira Costa por request.

Purpose: Entrega TEN-02 (gestão de usuários) e TEN-03 (contexto de empresa) no backend. Roda em paralelo ao frontend skeleton (Plan 02) — sem sobreposição de arquivos.
Output: Backend com rotas de admin/hierarchy/managers registradas e a tabela managers criada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-inherited-stack/01-CONTEXT.md
@.planning/phases/01-foundation-inherited-stack/01-RESEARCH.md
@.planning/phases/01-foundation-inherited-stack/01-PATTERNS.md
@.planning/phases/01-foundation-inherited-stack/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: admin.go (seletivo) + environment.go + hierarchy.go + managers.go + migração 003</name>
  <files>backend/handlers/admin.go, backend/handlers/environment.go, backend/handlers/hierarchy.go, backend/handlers/managers.go, backend/migrations/003_managers.sql</files>
  <read_first>
    - /home/claudiobezerra/projetos/FB_APU04/backend/handlers/admin.go (copiar SOMENTE List/Create/Promote/Delete/Reassign; PATTERNS.md § admin.go — funções a NÃO copiar listadas)
    - /home/claudiobezerra/projetos/FB_APU04/backend/handlers/environment.go (cópia integral; PATTERNS.md § environment.go)
    - /home/claudiobezerra/projetos/FB_APU04/backend/handlers/hierarchy.go (cópia integral; PATTERNS.md § hierarchy.go, nota P7 sobre import_jobs)
    - /home/claudiobezerra/projetos/FB_APU04/backend/handlers/managers.go (cópia integral; PATTERNS.md § managers.go)
    - /home/claudiobezerra/projetos/FB_APU04/backend/migrations/046_create_managers_table.sql (cópia direta; PATTERNS.md § migrations/003_managers.sql)
  </read_first>
  <action>
    Copiar `handlers/environment.go`, `handlers/hierarchy.go`, `handlers/managers.go` integralmente (sem imports de módulo interno → sem find/replace, mas confirmar com grep). Copiar `handlers/admin.go` APENAS com as funções `ListUsersHandler`, `CreateUserHandler`, `PromoteUserHandler`, `DeleteUserHandler`, `ReassignUserHandler`; NÃO copiar `ResetDatabaseHandler`, `ResetCompanyDataHandler`, `DiagnosticDataHandler`, `AdminNFCancelamentoHandler`, `RefreshViewsHandler` (dependem de tabelas SPED/mv_mercadorias inexistentes). Se algum import `fb_apu04/` aparecer em qualquer arquivo, fazer find/replace `"fb_apu04/`→`"fb_testesfc/` (Pitfall P1). Em `hierarchy.go`, manter o LEFT JOIN em `import_jobs` como está — a tabela não existe na Fase 1 mas o handler retorna `branches=[]` sem erro (P7); não criar a tabela. Copiar `migrations/003_managers.sql` direto de 046 (tabela `managers` + índices + trigger `update_managers_updated_at`).
  </action>
  <verify>
    <automated>cd /home/claudiobezerra/projetos/FB_TESTESFC/backend && ! grep -rn '"fb_apu04/' handlers/admin.go handlers/environment.go handlers/hierarchy.go handlers/managers.go && ! grep -q "ResetDatabaseHandler\|mv_mercadorias" handlers/admin.go && grep -q "CREATE TABLE IF NOT EXISTS managers" migrations/003_managers.sql && grep -q "GetEffectiveCompanyID" handlers/managers.go</automated>
  </verify>
  <acceptance_criteria>
    - `admin.go` contém os 5 handlers de usuário e nenhuma função de reset/diagnóstico SPED
    - `environment.go`, `hierarchy.go`, `managers.go` copiados; nenhum import `"fb_apu04/` remanescente
    - `migrations/003_managers.sql` cria a tabela `managers`
  </acceptance_criteria>
  <done>Handlers de gestão copiados (admin seletivo); migração 003 cria managers; sem dependências SPED fora de escopo.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar rotas de admin/hierarchy/managers no main.go</name>
  <files>backend/main.go</files>
  <read_first>
    - /home/claudiobezerra/projetos/FB_TESTESFC/backend/main.go (estado atual após Plan 01)
    - PATTERNS.md § backend/main.go (padrão de registro de rotas, incl. multi-method inline para /api/config/*)
  </read_first>
  <action>
    Acrescentar ao `main.go` (sem remover as rotas de auth já registradas pelo Plan 01) as rotas: admin via `withAuth(..., "admin")` — `/api/admin/users` (ListUsersHandler), `/api/admin/users/create` (CreateUserHandler), `/api/admin/users/promote` (PromoteUserHandler), `/api/admin/users/delete` (DeleteUserHandler), `/api/admin/users/reassign` (ReassignUserHandler); hierarquia via `withAuth(..., "")` usando o padrão multi-method inline (switch em r.Method para GET/POST/PUT/DELETE) para `/api/config/environments`, `/api/config/groups`, `/api/config/companies` mapeando aos handlers Get/Create/Update/Delete de environment.go; managers via `withAuth(..., "")` — `/api/managers` (ListManagersHandler), `/api/managers/create` (CreateManagerHandler), e `/api/managers/` para update/delete por id. Verificar que `go build ./...` continua passando.
  </action>
  <verify>
    <automated>cd /home/claudiobezerra/projetos/FB_TESTESFC/backend && go build ./... && grep -q '/api/admin/users' main.go && grep -q '/api/config/environments' main.go && grep -q '/api/managers' main.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` retorna exit 0 com os novos handlers referenciados
    - Rotas `/api/admin/users*`, `/api/config/{environments,groups,companies}`, `/api/managers*` registradas
    - Rotas de auth do Plan 01 permanecem registradas (não removidas)
  </acceptance_criteria>
  <done>Rotas de gestão registradas no main.go; backend compila inteiro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/admin/* | Operações privilegiadas (criar/deletar usuário) exigem role=admin |
| Browser → /api/config/*, /api/managers* | Operações escopadas por empresa via X-Company-ID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | /api/admin/users* | mitigate | `withAuth(handler, "admin")` — AuthMiddleware exige role=admin no JWT |
| T-03-02 | Tampering | Escopo de empresa | mitigate | `GetEffectiveCompanyID(db, userID, X-Company-ID)` valida o company contra vínculos do usuário antes de operar |
| T-03-03 | Information Disclosure | Listagem de usuários | mitigate | ListUsersHandler escopa por hierarquia do admin; senhas nunca retornadas (apenas hash interno) |
| T-03-04 | Tampering | Queries managers/environment | mitigate | Parâmetros `$1` via lib/pq em todos os handlers |
</threat_model>

<verification>
- `cd backend && go build ./...` → exit 0
- Com o backend rodando e migração 003 aplicada: `SELECT to_regclass('managers')` não é nulo
- `GET /api/admin/users` com Bearer de admin retorna 200; sem token retorna 401
</verification>

<success_criteria>
- Admin gerencia usuários via /api/admin/users* (TEN-02)
- Contexto de empresa resolvido por GetEffectiveCompanyID em handlers por empresa (TEN-03)
- Tabela managers criada pela migração 003
- Backend compila inteiro (go build ./...)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-inherited-stack/01-03-SUMMARY.md` when done
</output>
