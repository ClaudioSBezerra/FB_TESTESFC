---
phase: 01-foundation-inherited-stack
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - backend/go.mod
  - backend/go.sum
  - backend/handlers/erp_bridge.go
  - backend/migrations/002_erp_bridge.sql
  - backend/migrations/005_seed_erp_bridge_ferreira_costa.sql
  - backend/main.go
autonomous: false
requirements: [TEN-01]
user_setup: []
must_haves:
  truths:
    - "Tabela erp_bridge_config existe (migração 002) e tem linha para a empresa Ferreira Costa (migração 005) após todas as migrações rodarem"
    - "POST /api/erp-bridge/config salva credenciais Oracle criptografadas (AES-GCM)"
    - "POST /api/erp-bridge/test-connection abre+pinga Oracle via go-ora e retorna {ok:true|false}"
    - "Credenciais Oracle nunca são retornadas em claro ao frontend"
  artifacts:
    - path: "backend/handlers/erp_bridge.go"
      provides: "ERPBridgeConfigHandler + ERPBridgeTestConnectionHandler (novo)"
      contains: "ERPBridgeTestConnectionHandler"
    - path: "backend/migrations/002_erp_bridge.sql"
      provides: "Tabelas erp_bridge_config e correlatas (apenas DDL, sem seed)"
      contains: "erp_bridge_config"
    - path: "backend/migrations/005_seed_erp_bridge_ferreira_costa.sql"
      provides: "Linha idempotente erp_bridge_config para a empresa Ferreira Costa"
      contains: "erp_bridge_config"
  key_links:
    - from: "backend/handlers/erp_bridge.go"
      to: "sijms/go-ora/v2"
      via: "sql.Open(\"oracle\", dsn) + PingContext"
      pattern: "sijms/go-ora"
    - from: "backend/main.go"
      to: "handlers.ERPBridgeTestConnectionHandler"
      via: "withAuth /api/erp-bridge/test-connection"
      pattern: "test-connection"
---

<objective>
Adicionar a infra de conexão Oracle (ERP_BRIDGE) ao backend: migração 002 (tabelas erp_bridge), os handlers de configuração/credenciais por cópia seletiva, e o ENDPOINT NOVO `POST /api/erp-bridge/test-connection` que abre e pinga a conexão Oracle via `github.com/sijms/go-ora/v2` (puro-Go, CGO_ENABLED=0) — sem consultar prod/PRODB (isso é Fase 2, D-14). Inclui a inserção da linha `erp_bridge_config` para a empresa Ferreira Costa.

Purpose: Cumpre D-14 ("conexão Oracle operacional e testável") sem invadir a Fase 2. Roda em paralelo ao frontend de gestão (Plan 05) — sem sobreposição de arquivos.
Output: Backend com erp_bridge config + test-connection registrados e go-ora adicionado ao go.mod.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-inherited-stack/01-CONTEXT.md
@.planning/phases/01-foundation-inherited-stack/01-RESEARCH.md
@.planning/phases/01-foundation-inherited-stack/01-PATTERNS.md
@.planning/phases/01-foundation-inherited-stack/01-01-SUMMARY.md
@.planning/phases/01-foundation-inherited-stack/01-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verificar legitimidade do pacote go-ora/v2</name>
  <files>none — checkpoint humano, sem arquivos modificados</files>
  <action>Checkpoint humano de verificação de legitimidade de pacote — sem implementação automatizada. Siga <how-to-verify> e aprove antes de adicionar a dependência.</action>
  <what-built>Verificação de legitimidade do pacote Go novo `github.com/sijms/go-ora/v2@v2.9.0` (driver Oracle puro-Go) antes de adicioná-lo ao go.mod.</what-built>
  <how-to-verify>
    1. O RESEARCH.md § Auditoria de Legitimidade de Pacotes marca `github.com/sijms/go-ora/v2` como [ASSUMED] verificável (confirmado via proxy.golang.org v2.9.0, 2025-06-09).
    2. Confirme a existência e a versão: `curl -s "https://proxy.golang.org/github.com/sijms/go-ora/v2/@latest"` deve retornar `{"Version":"v2.9.0",...}`.
    3. Confirme a project page em https://github.com/sijms/go-ora (driver Oracle puro-Go, sem CGO).
    4. Esta é uma dependência de banco de dados nova — confirme que é a esperada antes de adicioná-la.
  </how-to-verify>
  <resume-signal>Digite "aprovado" para adicionar go-ora/v2 ao go.mod, ou indique uma alternativa.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: migração 002 + erp_bridge.go + endpoint test-connection + go-ora</name>
  <files>backend/go.mod, backend/go.sum, backend/handlers/erp_bridge.go, backend/migrations/002_erp_bridge.sql, backend/migrations/005_seed_erp_bridge_ferreira_costa.sql</files>
  <read_first>
    - /home/claudiobezerra/projetos/FB_APU04/backend/migrations/065_erp_bridge.sql (cópia direta; PATTERNS.md § migrations/002_erp_bridge.sql)
    - /home/claudiobezerra/projetos/FB_APU04/backend/handlers/erp_bridge.go (copiar Config/GenerateAPIKey/Credentials; PATTERNS.md § erp_bridge.go — funções a NÃO copiar listadas; helper erpBridgeGetCompany)
    - RESEARCH.md § D-14 RESOLVIDO (excerpt completo de ERPBridgeTestConnectionHandler) e § Log de Suposições A1 (DSN go-ora a verificar)
    - /home/claudiobezerra/projetos/FB_TESTESFC/backend/handlers/crypto.go (DecryptFieldWithFallback, do Plan 01)
  </read_first>
  <behavior>
    - test-connection com `oracle_dsn` vazio/ausente → resposta JSON `{"ok": false, "error": "DSN Oracle não configurado"}` (sem tentar abrir conexão)
    - test-connection com DSN configurado mas Oracle inacessível → `{"ok": false, "error": "<erro de dial/ping>"}` (não derruba o servidor)
    - test-connection sem company resolvível → `{"ok": false, "error": "empresa não encontrada"}`
    - Método != POST → 405 method not allowed
  </behavior>
  <action>
    Copiar `migrations/002_erp_bridge.sql` direto de 065 (tabelas `erp_bridge_config` PK company_id, `erp_bridge_runs`, `erp_bridge_run_items`, `erp_bridge_servidores`, `parceiros`). A migração 002 é APENAS DDL (CREATE TABLE) — NÃO inserir nenhuma linha de seed nela. A linha `erp_bridge_config` da Ferreira Costa vai em uma migração de seed SEPARADA, `005_seed_erp_bridge_ferreira_costa.sql`, contendo `INSERT INTO erp_bridge_config (company_id) SELECT id FROM companies WHERE name='Ferreira Costa' ON CONFLICT (company_id) DO NOTHING`. Razão da ordenação (execução alfabética em runtime: 001<002<003<004<005): quando a 005 roda, tanto a TABELA `erp_bridge_config` (criada pela 002) quanto a LINHA da empresa Ferreira Costa (semeada pela 004, owned pelo Plan 01) já existem — o SELECT acha a company e o INSERT funciona. NÃO colocar este INSERT na 002 (rodaria antes da 004 criar a empresa → SELECT retorna 0 linhas → linha nunca criada) NEM na 004 (a 004 é escrita no Wave 1 pelo Plan 01, mas a tabela `erp_bridge_config` só é criada pela 002 no Wave 3/Plan 04; no checkpoint Walking Skeleton do Wave 2 a 002 ainda não existe e a 004 falharia ao referenciar a tabela inexistente). A migração 005, owned por este plano (Wave 3), só passa a existir junto com a 002, mantendo o `docker compose up` do Wave 2 limpo (migrações presentes nesse ponto: 001, 003, 004 — sem nenhuma referência a erp_bridge). Copiar `handlers/erp_bridge.go` trazendo APENAS `ERPBridgeConfigHandler`, `ERPBridgeGenerateAPIKeyHandler`, `ERPBridgeCredentialsHandler` e o helper `erpBridgeGetCompany`; NÃO copiar `ERPBridgeRunsHandler`, `ERPBridgeRunHandler`, `ERPBridgeServidoresHandler`, `ERPBridgeRegistrarServidoresHandler`, `ERPBridgeTriggerHandler`, `ERPBridgePendingHandler`, `ERPBridgeHeartbeatHandler` (dependem de daemon/workers fora da fase). Find/replace `"fb_apu04/`→`"fb_testesfc/` se houver. Adicionar `import _ "github.com/sijms/go-ora/v2"` (registro do driver "oracle") e `"context"`/`"fmt"`/`"time"` se necessário. Escrever `ERPBridgeTestConnectionHandler(db *sql.DB) http.HandlerFunc` NOVO conforme RESEARCH.md § D-14: exige POST, resolve company via `erpBridgeGetCompany`, lê `oracle_dsn/oracle_usuario/oracle_senha` de `erp_bridge_config`, descriptografa via `DecryptFieldWithFallback`, monta DSN go-ora (`oracle://user:pass@host` — formato [ASSUMED A1]: VERIFICAR a sintaxe exata na doc do go-ora/v2 durante a implementação e ajustar; implementar fallback Easy Connect se a URL falhar), `sql.Open("oracle", dsn)`, `PingContext` com timeout 10s, retorna `{"ok": true}` ou `{"ok": false, "error": ...}`. Atualizar `go.mod` com `github.com/sijms/go-ora/v2 v2.9.0` e rodar `go mod tidy`.
  </action>
  <verify>
    <automated>cd /home/claudiobezerra/projetos/FB_TESTESFC/backend && go build ./... && grep -q "sijms/go-ora/v2" go.mod && grep -q "ERPBridgeTestConnectionHandler" handlers/erp_bridge.go && grep -q "DSN Oracle não configurado" handlers/erp_bridge.go && grep -q "CREATE TABLE IF NOT EXISTS erp_bridge_config" migrations/002_erp_bridge.sql && ! grep -qi "INSERT INTO erp_bridge_config" migrations/002_erp_bridge.sql && grep -qi "INSERT INTO erp_bridge_config" migrations/005_seed_erp_bridge_ferreira_costa.sql && grep -q "Ferreira Costa" migrations/005_seed_erp_bridge_ferreira_costa.sql && ! grep -q "ERPBridgeHeartbeatHandler\|ERPBridgeTriggerHandler" handlers/erp_bridge.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exit 0; `go.mod` contém `github.com/sijms/go-ora/v2 v2.9.0`
    - `migrations/002_erp_bridge.sql` cria as tabelas `erp_bridge_config` e correlatas (apenas DDL); `migrations/005_seed_erp_bridge_ferreira_costa.sql` garante a linha de `erp_bridge_config` da empresa Ferreira Costa
    - `ERPBridgeTestConnectionHandler` existe, trata DSN vazio com `{"ok":false,...}` e usa `sql.Open("oracle", ...)` + PingContext com timeout
    - Handlers de daemon/worker fora de escopo NÃO foram copiados
  </acceptance_criteria>
  <done>erp_bridge config + credenciais copiados, endpoint test-connection novo implementado com go-ora, migração 002 cria as tabelas e a linha de config da Ferreira Costa.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar rotas ERP Bridge no main.go</name>
  <files>backend/main.go</files>
  <read_first>
    - /home/claudiobezerra/projetos/FB_TESTESFC/backend/main.go (estado após Plans 01 e 03)
    - PATTERNS.md § backend/main.go (bloco ERP Bridge das rotas)
  </read_first>
  <action>
    Acrescentar ao `main.go` (sem remover rotas existentes) o bloco ERP Bridge: `/api/erp-bridge/config` via `withAuth(handlers.ERPBridgeConfigHandler, "")`, `/api/erp-bridge/config/generate-api-key` via `withAuth(handlers.ERPBridgeGenerateAPIKeyHandler, "admin")`, `/api/erp-bridge/test-connection` via `withAuth(handlers.ERPBridgeTestConnectionHandler, "")` (NOVO), e `/api/erp-bridge/credentials` via `withDB(handlers.ERPBridgeCredentialsHandler)` (para autenticação do daemon futuro). Confirmar `go build ./...`.
  </action>
  <verify>
    <automated>cd /home/claudiobezerra/projetos/FB_TESTESFC/backend && go build ./... && grep -q '/api/erp-bridge/test-connection' main.go && grep -q '/api/erp-bridge/config' main.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exit 0
    - Rotas `/api/erp-bridge/config`, `/api/erp-bridge/test-connection`, `/api/erp-bridge/credentials` registradas
    - Rotas dos Plans 01 e 03 permanecem registradas
  </acceptance_criteria>
  <done>Rotas ERP Bridge registradas no main.go; backend compila inteiro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/erp-bridge/* | Credenciais Oracle (DSN/usuário/senha) cruzam; exigem autenticação |
| API → Oracle (externo) | Conexão de teste sai do servidor para host Oracle informado pelo usuário |
| erp_bridge_config (Postgres) | Credenciais Oracle persistidas criptografadas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Exfiltração de credenciais Oracle via API | mitigate | `oracle_senha` NUNCA retornado ao frontend (apenas flag *_set); AES-256-GCM em crypto.go; descriptografia só server-side |
| T-04-02 | Information Disclosure | Erro do test-connection vazando segredos | mitigate | Mensagem de erro do Ping não inclui a senha; DSN montado server-side e descartado após o teste |
| T-04-03 | Spoofing/SSRF | test-connection abre conexão a host arbitrário | mitigate | Endpoint exige autenticação (withAuth); usa apenas o DSN previamente salvo em erp_bridge_config da empresa do usuário, não um host vindo no corpo da requisição; timeout 10s limita abuso |
| T-04-04 | Tampering | go.mod/go.sum (supply chain) | mitigate | go-ora/v2@v2.9.0 verificado via proxy.golang.org (checkpoint blocking-human); `go mod tidy` com go.sum; T-01-SC |
| T-04-05 | Elevation of Privilege | generate-api-key | mitigate | `withAuth(..., "admin")` restringe geração de API key a admin |
</threat_model>

<verification>
- `cd backend && go build ./...` → exit 0
- Migração 002 aplicada: `SELECT to_regclass('erp_bridge_config')` não nulo (tabela criada); migração 005 aplicada: existe linha em `erp_bridge_config` para a empresa Ferreira Costa após todas as migrações
- `POST /api/erp-bridge/test-connection` autenticado com DSN vazio retorna `{"ok":false,"error":"DSN Oracle não configurado"}`
</verification>

<success_criteria>
- Tabelas erp_bridge criadas e linha de config da Ferreira Costa presente (apoia TEN-01)
- Credenciais Oracle salvas criptografadas e nunca retornadas em claro
- test-connection abre+pinga Oracle via go-ora sem consultar prod/PRODB (D-14)
- Backend compila inteiro (go build ./...)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-inherited-stack/01-04-SUMMARY.md` when done
</output>
