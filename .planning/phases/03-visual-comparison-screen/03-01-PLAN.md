---
phase: 03-visual-comparison-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/handlers/fiscal_comparison.go
  - backend/main.go
  - frontend/src/pages/ComparacaoFiscal.tsx
  - frontend/src/lib/navigation.ts
  - frontend/src/App.tsx
autonomous: true
requirements: [CMP-01, CMP-02]
must_haves:
  truths:
    - "Usuário abre a aba 'Comparação Fiscal' e vê uma lista de itens de todas as notas da empresa"
    - "Cada item mostra, por imposto (ICMS, ICMS-ST, PIS, COFINS), três colunas: Esperado, Calculado e Diferença"
    - "Itens com qualquer diferença ≠ 0 em qualquer dos 4 pares são destacados visualmente (vermelho)"
    - "Itens ainda sem cálculo fiscal concluído (status != ok) aparecem com badge 'Não calculado' e não são marcados como divergentes"
  artifacts:
    - path: "backend/handlers/fiscal_comparison.go"
      provides: "FiscalComparisonListHandler — GET /api/fiscal-comparison, JOIN esperado+calculado escopado por company_id"
      exports: ["FiscalComparisonListHandler"]
      min_lines: 90
    - path: "frontend/src/pages/ComparacaoFiscal.tsx"
      provides: "Página de lista item a item com colunas esperado/calculado/diferença e destaque de divergência"
      min_lines: 120
    - path: "frontend/src/lib/navigation.ts"
      provides: "Aba 'Comparação Fiscal' registrada no módulo config"
      contains: "comparacao-fiscal"
    - path: "frontend/src/App.tsx"
      provides: "Rota protegida /importacoes/comparacao-fiscal"
      contains: "ComparacaoFiscal"
  key_links:
    - from: "frontend/src/pages/ComparacaoFiscal.tsx"
      to: "/api/fiscal-comparison"
      via: "fetch em useQuery com auth headers"
      pattern: "fetch\\(.*api/fiscal-comparison"
    - from: "backend/handlers/fiscal_comparison.go"
      to: "nfe_saidas_itens + fiscal_execution_items"
      via: "JOIN escopado por company_id"
      pattern: "LEFT JOIN fiscal_execution_items"
    - from: "backend/main.go"
      to: "handlers.FiscalComparisonListHandler"
      via: "http.HandleFunc withAuth"
      pattern: "/api/fiscal-comparison"
---

<objective>
Entrega a fatia vertical mínima e navegável da tela de Comparação Fiscal: um endpoint de leitura que faz o JOIN entre os valores esperados (XML, `nfe_saidas_itens`) e calculados (pacote fiscal, `fiscal_execution_items`) escopado por `company_id`, mais uma nova página React que lista os itens de todas as notas com as colunas Esperado | Calculado | Diferença para os 4 pares comparáveis (ICMS, ICMS-ST, PIS, COFINS) e destaca visualmente os itens divergentes. A aba é registrada na navegação e na rota, de modo que ao final deste plano o usuário consegue de fato abrir "Comparação Fiscal" e ver a comparação item a item — cobre CMP-01 e CMP-02 no nível da lista.

Purpose: É o core value do projeto (validar o pacote fiscal). Esta fatia prova o caminho ponta a ponta (DB → API → UI) com dados reais já persistidos pela Fase 2.
Output: `fiscal_comparison.go` (handler + rota), `ComparacaoFiscal.tsx` (lista), aba em `navigation.ts` e rota em `App.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visual-comparison-screen/03-CONTEXT.md
@.planning/phases/03-visual-comparison-screen/03-PATTERNS.md

<interfaces>
<!-- Contratos extraídos do codebase — o executor NÃO precisa explorar para descobri-los. -->

Auth/company-scoping (backend/handlers/erp_bridge.go:43-50) — usar verbatim no novo handler:
  companyID, err := erpBridgeGetCompany(db, r)  // resolve company_id do JWT (nunca do cliente)
  jsonErr(w, statusCode, "msg")                 // helper compartilhado do pacote handlers (já existe)

Schema ESPERADO — nfe_saidas_itens (migration 007), colunas relevantes:
  id (uuid), nfe_id, company_id, n_item, c_prod, x_prod, ncm, cfop,
  v_bc_icms, v_icms, v_bc_st, v_st, v_bc_pis, v_pis, v_bc_cofins, v_cofins  (NUMERIC(15,2))

Schema CALCULADO — fiscal_execution_items (migration 008), colunas relevantes:
  nfe_item_id (FK → nfe_saidas_itens.id), company_id, status (pending|ok|error|sem_grupo_fiscal), error_message,
  base_calculo_icms, valor_icms, base_substituicao, valor_substituicao,
  base_calculo_pis, valor_pis, base_calculo_cofins, valor_cofins  (NUMERIC(15,2), NULL quando não calculado)
  Mapeamento ICMS-ST (D-04 4º par): base_substituicao/valor_substituicao ARE ICMS-ST.

Schema NOTA — nfe_saidas (migration 006), colunas relevantes:
  id, company_id, numero_nfe, serie, dest_nome, dest_cnpj_cpf, data_emissao (DATE)

Frontend auth/fetch (ConsultaNFeSaidas.tsx:408-421):
  const { token, companyId } = useAuth();
  headers: { Authorization: `Bearer ${token}`, 'X-Company-ID': companyId || '' }
  useQuery({ queryKey: ['...', companyId], enabled: !!token && !!companyId })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Endpoint GET /api/fiscal-comparison (JOIN esperado x calculado, escopado por company_id)</name>
  <files>backend/handlers/fiscal_comparison.go, backend/main.go</files>
  <read_first>
    - backend/handlers/nfe_saidas.go (linhas 593-660 NFeSaidasListHandler e 660-793 NFeSaidaDetailHandler — copiar método-guard, erpBridgeGetCompany, padrão de scan/envelope e do JOIN LEFT com fiscal_execution_items)
    - backend/handlers/erp_bridge.go (linhas 43-50 — erpBridgeGetCompany)
    - backend/migrations/007_nfe_saidas_itens.sql (colunas esperadas)
    - backend/migrations/008_fiscal_execution_items.sql (colunas calculadas + status)
    - backend/migrations/006_nfe_saidas.sql (colunas numero_nfe, serie, dest_nome, dest_cnpj_cpf, data_emissao)
    - backend/main.go (linhas 350-358 bloco de registro de rotas withAuth)
  </read_first>
  <action>
    Criar `backend/handlers/fiscal_comparison.go` no pacote `handlers` com a função exportada `FiscalComparisonListHandler(db *sql.DB) http.HandlerFunc`, seguindo verbatim o padrão de `NFeSaidasListHandler`: header `Content-Type: application/json`; método-guard rejeitando != GET com `jsonErr(w, http.StatusMethodNotAllowed, "Método não permitido")`; resolver company via `companyID, err := erpBridgeGetCompany(db, r)` e retornar `jsonErr(w, http.StatusUnauthorized, "Não autenticado")` em erro.

    Definir uma struct de linha `comparisonItemRow` com tags JSON exatas. Campos de identificação: `item_id`(string), `n_item`(int), `x_prod`(string), `ncm`(string), `cfop`(string), `nfe_id`(string), `numero_nfe`(string), `serie`(string), `dest_nome`(string), `dest_cnpj_cpf`(string), `data_emissao`(string). Campos esperados (float64): `esp_bc_icms`, `esp_icms`, `esp_bc_st`, `esp_st`, `esp_bc_pis`, `esp_pis`, `esp_bc_cofins`, `esp_cofins`. Campos calculados como `*float64` (ponteiro, JSON null quando não calculado): `calc_bc_icms`, `calc_icms`, `calc_bc_st`, `calc_st`, `calc_bc_pis`, `calc_pis`, `calc_bc_cofins`, `calc_cofins`. Status: `fiscal_status`(string, "" quando sem linha em fiscal_execution_items), `error_message`(string).

    Query parametrizada (usar `$1` = companyID): SELECT de `nfe_saidas_itens i` com `JOIN nfe_saidas n ON n.id = i.nfe_id` e `LEFT JOIN fiscal_execution_items f ON f.nfe_item_id = i.id`, colunas: `i.id, i.n_item, i.x_prod, COALESCE(i.ncm,''), COALESCE(i.cfop,''), n.id, COALESCE(n.numero_nfe,''), COALESCE(n.serie,''), COALESCE(n.dest_nome,''), COALESCE(n.dest_cnpj_cpf,''), TO_CHAR(n.data_emissao,'DD/MM/YYYY'), i.v_bc_icms, i.v_icms, i.v_bc_st, i.v_st, i.v_bc_pis, i.v_pis, i.v_bc_cofins, i.v_cofins, f.base_calculo_icms, f.valor_icms, f.base_substituicao, f.valor_substituicao, f.base_calculo_pis, f.valor_pis, f.base_calculo_cofins, f.valor_cofins, COALESCE(f.status,''), COALESCE(f.error_message,'')`. `WHERE i.company_id = $1` (D-02: todas as notas juntas; nunca aceitar company do cliente). `ORDER BY n.data_emissao DESC, n.numero_nfe DESC, i.n_item ASC`. Adicionar `LIMIT 2000` (guarda de payload, coerente com o LIMIT 500 do list de notas).

    Fazer scan das colunas calculadas em `sql.NullFloat64` e converter para `*float64` (nil quando `!Valid`) antes de append — assim itens sem cálculo emitem null e o frontend os classifica como "Não calculado" (D-10). Em erro de scan, `log.Printf(...); continue` (não abortar o lote). Em erro de query, `log.Printf("FiscalComparisonList query error (company %s): %v", companyID, err)` + `jsonErr(w, http.StatusInternalServerError, "Erro ao consultar comparação")` (nunca vazar `err.Error()` ao cliente). Encerrar com `json.NewEncoder(w).Encode(map[string]interface{}{"total": len(list), "items": list})`, inicializando `list := []comparisonItemRow{}` (slice não-nil → `[]` e não `null`).

    Em `backend/main.go`, adicionar junto ao bloco de rotas withAuth (linhas 350-358): `http.HandleFunc("/api/fiscal-comparison", withAuth(handlers.FiscalComparisonListHandler, ""))` (role vazia = qualquer usuário autenticado, mesmo tier de `/api/nfe-saidas`).
  </action>
  <verify>
    <automated>go -C backend build ./... && go -C backend vet ./handlers/</automated>
  </verify>
  <acceptance_criteria>
    - `go -C backend build ./...` compila sem erros
    - `grep -n "func FiscalComparisonListHandler" backend/handlers/fiscal_comparison.go` retorna 1 linha
    - `grep -c "WHERE i.company_id = \$1" backend/handlers/fiscal_comparison.go` >= 1 (scoping por JWT, não por cliente)
    - `grep -q "/api/fiscal-comparison" backend/main.go` verdadeiro e a linha usa `withAuth(handlers.FiscalComparisonListHandler, \"\")`
    - Handler NÃO lê company_id de query string, body ou header `X-Company-ID` diretamente — só via `erpBridgeGetCompany`
    - Colunas calculadas (`calc_*`) são `*float64` e viram `nil` quando `sql.NullFloat64.Valid == false`
  </acceptance_criteria>
  <done>Endpoint compila, está registrado com autenticação, escopa por company_id do JWT e retorna itens com pares esperado/calculado (calculados null quando não processados).</done>
</task>

<task type="auto">
  <name>Task 2: Página ComparacaoFiscal.tsx (lista item a item com Esperado|Calculado|Diferença + destaque) e wiring de navegação/rota</name>
  <files>frontend/src/pages/ComparacaoFiscal.tsx, frontend/src/lib/navigation.ts, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/pages/ConsultaNFeSaidas.tsx (arquivo inteiro — usar como template estrutural: imports, useAuth+react-query, FiscalStatusBadge/FISCAL_STATUS_META linhas 123-161, fmtBRL linhas 166-169, tabela + linhas 561-619)
    - frontend/src/lib/navigation.ts (array modules.config.tabs)
    - frontend/src/App.tsx (imports topo + bloco <Routes> protegido, rota /importacoes/notas-saida)
    - backend/handlers/fiscal_comparison.go (contrato JSON do endpoint criado na Task 1 — chaves esp_*, calc_*, fiscal_status)
  </read_first>
  <action>
    Criar `frontend/src/pages/ComparacaoFiscal.tsx` (default export `ComparacaoFiscal`) reaproveitando os imports de `ConsultaNFeSaidas.tsx` (useState, useMemo, useQuery, useAuth, Card, Button, Badge, Table*, Tooltip*). Copiar verbatim o helper `fmtBRL` (ConsultaNFeSaidas.tsx:166-169).

    Definir o tipo `ComparisonItemRow` espelhando o contrato da Task 1 (chaves `item_id, n_item, x_prod, ncm, cfop, nfe_id, numero_nfe, serie, dest_nome, dest_cnpj_cpf, data_emissao, esp_bc_icms..esp_cofins, calc_bc_icms..calc_cofins` como `number | null`, `fiscal_status`, `error_message`).

    Data fetching com `useQuery<{ total: number; items: ComparisonItemRow[] }>` keyed por `['fiscal-comparison', companyId]`, fetch de `/api/fiscal-comparison` com headers `{ Authorization: Bearer ${token}, 'X-Company-ID': companyId || '' }`, `enabled: !!token && !!companyId` (padrão ConsultaNFeSaidas.tsx:408-421).

    Definir e EXPORTAR duas funções puras (contrato reutilizado pelo Plano 03-02):
    - `pairDiff(esperado: number, calculado: number | null): number | null` → `calculado == null ? null : calculado - esperado`.
    - `isDivergente(item: ComparisonItemRow): boolean` → true se `item.fiscal_status === 'ok'` E qualquer um dos 4 pares tem `diff !== 0` (D-06: qualquer diferença ≠ 0, sem tolerância). Pares: (esp_bc_icms/calc_bc_icms, esp_icms/calc_icms), (esp_bc_st/calc_bc_st, esp_st/calc_st), (esp_bc_pis/calc_bc_pis, esp_pis/calc_pis), (esp_bc_cofins/calc_bc_cofins, esp_cofins/calc_cofins). Se `fiscal_status !== 'ok'` retorna false (D-10: "Não calculado" não conta como divergente).
    - `itemBucket(item): 'ok' | 'divergente' | 'nao_calculado'` → `fiscal_status !== 'ok'` ⇒ 'nao_calculado'; senão `isDivergente(item)` ? 'divergente' : 'ok'.

    Renderizar um badge de 3 estados reutilizando a convenção de cores de `FISCAL_STATUS_META` (D-06/discretion): 'ok' → `bg-green-50 text-green-700 border-green-200` label "OK"; 'divergente' → `bg-red-50 text-red-700 border-red-200` label "Divergente"; 'nao_calculado' → `bg-yellow-50 text-yellow-700 border-yellow-200` label "Não calculado" (com Tooltip explicando que o cálculo fiscal ainda não foi concluído / status). Divergente usa vermelho, não âmbar (é o alerta primário do validador).

    Tabela (D-04/D-05): colunas de identificação (Nº nota, Cliente=`dest_nome`, Item=`n_item`, Produto=`x_prod`, Status/badge) seguidas de 4 blocos de imposto — ICMS, ICMS-ST, PIS, COFINS — cada bloco com 3 subcolunas Esperado | Calculado | Diferença usando `fmtBRL`. Para cada bloco usar o par de VALOR do imposto (esp_icms/calc_icms; esp_st/calc_st; esp_pis/calc_pis; esp_cofins/calc_cofins) como coluna comparada na lista principal (as bases ficam para o Dialog do Plano 03-02). Diferença = `pairDiff(esperado, calculado)`, exibida com `fmtBRL` e classe `text-red-700 font-bold` quando `!= 0` e `!= null`. Linha (`TableRow`) recebe `className` com `bg-red-50` quando `itemBucket(item) === 'divergente'`. Calculado null ⇒ mostrar "—". A tabela é larga; envolver em container com `overflow-x-auto` (responsividade — discretion).

    Estados de carregamento/erro/vazio: "Carregando...", mensagem de erro amigável, e empty-state "Nenhum item para comparar — importe e execute notas na Fase de importação." quando `items.length === 0`.

    Wiring — em `frontend/src/lib/navigation.ts` adicionar ao array `modules.config.tabs`, logo após a entrada 'Notas Importadas': `{ label: 'Comparação Fiscal', path: '/importacoes/comparacao-fiscal' }` (sem `adminOnly` — disponível a todos autenticados, mesmo tier de Notas Importadas; `getActiveModule` já trata `/importacoes/*`). Em `frontend/src/App.tsx` adicionar o import `import ComparacaoFiscal from './pages/ComparacaoFiscal'` no topo e, dentro do bloco `<Routes>` protegido (após a rota `/importacoes/notas-saida`), `<Route path="/importacoes/comparacao-fiscal" element={<ProtectedRoute><ComparacaoFiscal /></ProtectedRoute>} />`.
  </action>
  <verify>
    <automated>npm --prefix frontend run build && npx --prefix frontend tsc --noEmit -p frontend/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix frontend run build` conclui sem erros
    - `npx --prefix frontend tsc --noEmit -p frontend/tsconfig.json` não reporta erros de tipo
    - `grep -q "api/fiscal-comparison" frontend/src/pages/ComparacaoFiscal.tsx` verdadeiro
    - `grep -q "export function isDivergente" frontend/src/pages/ComparacaoFiscal.tsx` e `grep -q "export function pairDiff" frontend/src/pages/ComparacaoFiscal.tsx` verdadeiros
    - `grep -q "comparacao-fiscal" frontend/src/lib/navigation.ts` e `grep -q "comparacao-fiscal" frontend/src/App.tsx` verdadeiros
    - `grep -q "import ComparacaoFiscal" frontend/src/App.tsx` verdadeiro
    - A lógica de divergência usa diferença ≠ 0 sem tolerância de arredondamento (D-06) e trata `fiscal_status !== 'ok'` como "Não calculado" (D-10), não como divergente
  </acceptance_criteria>
  <done>Build e typecheck passam; a aba "Comparação Fiscal" existe, roteia para a nova página, que busca `/api/fiscal-comparison` e renderiza a lista item a item com Esperado|Calculado|Diferença por imposto e destaque vermelho nos itens divergentes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → API (`/api/fiscal-comparison`) | Requisição autenticada por JWT; entrada não confiável (headers, company_id do cliente) |
| API → Postgres | Query parametrizada sobre tabelas do app (somente leitura nesta fase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | GET /api/fiscal-comparison | mitigate | `withAuth(..., "")` — rota exige JWT válido (AuthMiddleware); sem token → 401 |
| T-03-02 | Information Disclosure | company scoping | mitigate | `companyID` sempre de `erpBridgeGetCompany` (JWT); `WHERE i.company_id = $1`; nunca ler company do cliente |
| T-03-03 | Tampering | SQL query | mitigate | query 100% parametrizada (`$1`), sem concatenação de string |
| T-03-04 | Information Disclosure | mensagens de erro | mitigate | `log.Printf` interno + `jsonErr` genérico; nunca retornar `err.Error()` ao cliente |
| T-03-05 | Denial of Service | payload da lista | mitigate | `LIMIT 2000` na query; lista lean (sem full_result nesta fase) |
| T-03-SC | Tampering | npm/go installs | accept | Nenhum pacote novo instalado — reusa react-query, shadcn/ui, go-ora já auditados nas Fases 1-2 |
</threat_model>

<verification>
- `go -C backend build ./...` e `go -C backend vet ./handlers/` passam.
- `npm --prefix frontend run build` e `npx --prefix frontend tsc --noEmit -p frontend/tsconfig.json` passam.
- Checkpoint humano confirma a lista renderizando dados reais com divergências destacadas.
</verification>

<success_criteria>
- Endpoint `/api/fiscal-comparison` autenticado, escopado por company_id do JWT, retornando pares esperado/calculado por item (CMP-01).
- Página "Comparação Fiscal" navegável mostrando item a item as colunas Esperado | Calculado | Diferença para ICMS, ICMS-ST, PIS, COFINS, com divergências destacadas (CMP-02) e itens sem cálculo marcados "Não calculado".
</success_criteria>

<output>
Create `.planning/phases/03-visual-comparison-screen/03-01-SUMMARY.md` when done
</output>
