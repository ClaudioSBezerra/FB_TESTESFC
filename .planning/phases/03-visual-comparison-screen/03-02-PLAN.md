---
phase: 03-visual-comparison-screen
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - backend/handlers/fiscal_comparison.go
  - backend/main.go
  - frontend/src/pages/ComparacaoFiscal.tsx
autonomous: true
requirements: [CMP-03, CMP-04]
must_haves:
  truths:
    - "Usuário liga um toggle 'Só divergentes' e a lista passa a mostrar apenas itens com ao menos uma diferença ≠ 0"
    - "Itens 'Não calculado' nunca aparecem quando o toggle 'Só divergentes' está ligado"
    - "Cards no topo mostram Total de itens, itens OK, itens Divergentes e itens Não calculados, respeitando o filtro atual"
    - "Clicar num item abre um Dialog de detalhe com as 4 comparações (Base+Valor) Esperado|Calculado|Diferença e uma seção 'Só calculado' com DIFAL/FCP e demais campos do pacote fiscal"
    - "O Dialog mostra um resumo da nota do item (contagem OK/Divergente/Não calculado dos itens daquela nota)"
  artifacts:
    - path: "frontend/src/pages/ComparacaoFiscal.tsx"
      provides: "Toggle 'só divergentes', cards de resumo global, Dialog de detalhe com seção 'Só calculado' e resumo por nota"
      contains: "somenteDivergentes"
    - path: "backend/handlers/fiscal_comparison.go"
      provides: "FiscalComparisonDetailHandler — GET /api/fiscal-comparison/{id} com full_result + DIFAL/FCP escopado por company_id"
      exports: ["FiscalComparisonDetailHandler"]
  key_links:
    - from: "frontend/src/pages/ComparacaoFiscal.tsx"
      to: "/api/fiscal-comparison/{id}"
      via: "fetch no Dialog de detalhe"
      pattern: "fetch\\(.*api/fiscal-comparison/"
    - from: "backend/handlers/fiscal_comparison.go"
      to: "fiscal_execution_items.full_result"
      via: "SELECT do item por id escopado por company_id"
      pattern: "full_result"
    - from: "frontend/src/pages/ComparacaoFiscal.tsx"
      to: "displayItems (filtrados)"
      via: "useMemo sobre somenteDivergentes + cards derivados dos buckets"
      pattern: "somenteDivergentes"
---

<objective>
Refina a fatia entregue no Plano 03-01 com as capacidades que dependem da lista já existir: o filtro "só divergentes" (CMP-03), os cards de resumo global por bucket OK/Divergente/Não calculado (CMP-04), e o Dialog de detalhe por item (D-03/D-05/D-07) que mostra as comparações Base+Valor lado a lado e uma seção "Só calculado" com os campos do pacote fiscal sem par no XML (DIFAL, FCP e demais de `full_result`), além de um resumo da nota do item.

Purpose: Completa os requisitos de análise rápida do validador — filtrar ruído, quantificar divergências e auditar campo a campo o retorno do pacote fiscal.
Output: endpoint de detalhe `/api/fiscal-comparison/{id}`, e a página `ComparacaoFiscal.tsx` estendida com toggle, cards e Dialog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visual-comparison-screen/03-CONTEXT.md
@.planning/phases/03-visual-comparison-screen/03-PATTERNS.md
@.planning/phases/03-visual-comparison-screen/03-01-SUMMARY.md

<interfaces>
<!-- Contratos do Plano 03-01 (mesmo arquivo/endpoint) e do codebase -->

Do Plano 03-01 (frontend/src/pages/ComparacaoFiscal.tsx) — reutilizar as funções puras já exportadas:
  pairDiff(esperado, calculado): number | null
  isDivergente(item): boolean          // diff != 0 em qualquer par E fiscal_status === 'ok' (D-06)
  itemBucket(item): 'ok' | 'divergente' | 'nao_calculado'   // D-10
  fmtBRL(v): string
  tipo ComparisonItemRow (esp_*, calc_*, fiscal_status, nfe_id, numero_nfe, ...)

Helpers de Dialog a reutilizar de ConsultaNFeSaidas.tsx:191-218 (Secao, Linha, LinhaBRL) e o padrão DetalheNFe (linhas 220-393): Dialog open onOpenChange, DialogContent className="max-w-3xl max-h-[85vh] overflow-y-auto".

Schema IDENTIFICAÇÃO — nfe_saidas (migration 006): PK é `id` (UUID). NÃO existe coluna `nfe_id` nesta tabela. A FK item→nota vive em `nfe_saidas_itens.nfe_id`. Portanto o JOIN é `n.id = i.nfe_id` e a chave JSON `nfe_id` do response vem de `n.id` via alias `n.id AS nfe_id` (mesmo padrão do Plano 03-01).

Colunas dedicadas p/ "Só calculado" (fiscal_execution_items, migration 008):
  percentual_difal (NUMERIC 7,4), valor_icms_partilha_destino (DIFAL), valor_icms_pobreza (FCP),
  base_calculo_icms, valor_icms, base_substituicao, valor_substituicao,
  base_calculo_pis, valor_pis, base_calculo_cofins, valor_cofins,
  full_result (JSONB, ~88 campos), input_params (JSONB), grupo_fiscal_codigo (TEXT), status, error_message
  ACHADO (03-PATTERNS.md): nfe_saidas_itens NÃO tem coluna DIFAL/FCP por item → DIFAL/FCP entram só na seção "Só calculado" (D-07), sem par esperado.

FiscalResult (backend/services/oracle_fiscal.go) — fonte dos nomes/tipos dos ~88 campos de full_result (para mapear labels amigáveis na seção "Só calculado" — discretion).

Path-param parse (nfe_saidas.go:713-717): strings.TrimPrefix(r.URL.Path, "/api/nfe-saidas/") + TrimSuffix "/"; sql.ErrNoRows → 404.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Endpoint de detalhe GET /api/fiscal-comparison/{id} (full_result + DIFAL/FCP, escopado por company_id)</name>
  <files>backend/handlers/fiscal_comparison.go, backend/main.go</files>
  <read_first>
    - backend/handlers/fiscal_comparison.go (handler de lista criado no Plano 03-01 — reutilizar struct de identificação, erpBridgeGetCompany, padrão de erro; note que a query de lista usa `n.id` e mapeia para a chave JSON `nfe_id`)
    - backend/handlers/nfe_saidas.go (linhas 697-758 NFeSaidaDetailHandler — parse de path id via strings.TrimPrefix/TrimSuffix, WHERE id=$1 AND company_id=$2, sql.ErrNoRows → 404)
    - backend/migrations/006_nfe_saidas.sql (confirma que a PK de `nfe_saidas` é `id`; NÃO há coluna `nfe_id` nesta tabela)
    - backend/migrations/008_fiscal_execution_items.sql (colunas dedicadas + full_result + input_params)
    - backend/services/oracle_fiscal.go (FiscalResult — nomes dos campos de full_result)
    - backend/main.go (bloco de rotas withAuth linhas 350-358; note a convenção de rota com trailing slash como `/api/nfe-saidas/`)
  </read_first>
  <action>
    Adicionar em `backend/handlers/fiscal_comparison.go` a função exportada `FiscalComparisonDetailHandler(db *sql.DB) http.HandlerFunc`. Header JSON; método-guard != GET → 405; `companyID, err := erpBridgeGetCompany(db, r)` → 401 em erro. Extrair o id do path: `id := strings.TrimPrefix(r.URL.Path, "/api/fiscal-comparison/"); id = strings.TrimSuffix(id, "/")`; se vazio → `jsonErr(w, http.StatusBadRequest, "ID do item não informado")` (adicionar import `strings`).

    Query `db.QueryRow` sobre `nfe_saidas_itens i JOIN nfe_saidas n ON n.id = i.nfe_id LEFT JOIN fiscal_execution_items f ON f.nfe_item_id = i.id` com `WHERE i.id = $1 AND i.company_id = $2` (IDOR mitigation: escopar por company_id além do id; nunca só por id). Selecionar: identificação (i.id, i.n_item, i.x_prod, ncm, cfop, n.numero_nfe, n.serie, n.dest_nome, `n.id AS nfe_id` — a tabela `nfe_saidas` tem PK `id`, NÃO existe coluna `nfe_id`; use o alias `n.id AS nfe_id` para a chave JSON `nfe_id`, exatamente como faz a query de lista do Plano 03-01), os 8 valores esperados (v_bc_icms, v_icms, v_bc_st, v_st, v_bc_pis, v_pis, v_bc_cofins, v_cofins) e os campos calculados: `f.status`, `f.error_message`, `f.grupo_fiscal_codigo`, os 8 pares calculados (base_calculo_icms, valor_icms, base_substituicao, valor_substituicao, base_calculo_pis, valor_pis, base_calculo_cofins, valor_cofins), `f.percentual_difal`, `f.valor_icms_partilha_destino`, `f.valor_icms_pobreza`, e `f.full_result` (JSONB). Campos calculados nullable → `sql.NullFloat64`/`sql.NullString` → converter para `*float64`/`*string` no JSON. `full_result` → escanear como `[]byte` e reemitir como `json.RawMessage` no response (para o frontend acessar os ~88 campos). `sql.ErrNoRows` → `jsonErr(w, http.StatusNotFound, "Item não encontrado")`. Outro erro → `log.Printf("FiscalComparisonDetail query error (id %s, company %s): %v", id, companyID, err)` + 500 genérico.

    Response: `json.NewEncoder(w).Encode(...)` de uma struct com as chaves JSON (identificação, incluindo `nfe_id` proveniente de `n.id` + `esp_*` + `calc_*` + `percentual_difal` + `valor_icms_partilha_destino` + `valor_icms_pobreza` + `grupo_fiscal_codigo` + `fiscal_status` + `error_message` + `full_result` como `json.RawMessage`).

    Em `backend/main.go`, registrar a rota de detalhe com trailing slash (padrão `/api/nfe-saidas/`): `http.HandleFunc("/api/fiscal-comparison/", withAuth(handlers.FiscalComparisonDetailHandler, ""))`, ao lado da rota de lista do Plano 03-01.
  </action>
  <verify>
    <automated>go -C backend build ./... && go -C backend vet ./handlers/</automated>
  </verify>
  <acceptance_criteria>
    - `go -C backend build ./...` compila sem erros
    - `grep -n "func FiscalComparisonDetailHandler" backend/handlers/fiscal_comparison.go` retorna 1 linha
    - `grep -c "i.company_id = \$2\|company_id = \$2" backend/handlers/fiscal_comparison.go` >= 1 (detalhe escopado por company, não só por id — IDOR)
    - `grep -q "n.id AS nfe_id\|n\.id " backend/handlers/fiscal_comparison.go` verdadeiro (usa `n.id`, NUNCA `n.nfe_id` — coluna inexistente)
    - `grep -c "n\.nfe_id" backend/handlers/fiscal_comparison.go` == 0 fora do predicado do JOIN `n.id = i.nfe_id` (a coluna `nfe_id` só existe em `nfe_saidas_itens`, não em `nfe_saidas`)
    - `grep -q "full_result" backend/handlers/fiscal_comparison.go` verdadeiro e é emitido como json.RawMessage
    - `grep -q "\"/api/fiscal-comparison/\"" backend/main.go` verdadeiro (rota com trailing slash) usando `FiscalComparisonDetailHandler`
    - `sql.ErrNoRows` tratado como 404, não como 500
  </acceptance_criteria>
  <done>Endpoint de detalhe compila, registrado com autenticação, retorna full_result + DIFAL/FCP de um item escopado por company_id, com 404 para item inexistente/de outra empresa.</done>
</task>

<task type="auto">
  <name>Task 2: Toggle 'só divergentes', cards de resumo e Dialog de detalhe (Base+Valor + seção 'Só calculado' + resumo por nota)</name>
  <files>frontend/src/pages/ComparacaoFiscal.tsx</files>
  <read_first>
    - frontend/src/pages/ComparacaoFiscal.tsx (página criada no Plano 03-01 — reutilizar ComparisonItemRow, isDivergente, pairDiff, itemBucket, fmtBRL, badge)
    - frontend/src/pages/ConsultaNFeSaidas.tsx (linhas 191-218 Secao/Linha/LinhaBRL; 220-393 DetalheNFe padrão de Dialog+useQuery; 529-544 cards de resumo grid; 431-451 filtro client-side via useMemo)
    - backend/handlers/fiscal_comparison.go (contrato do endpoint de detalhe da Task 1 — chaves full_result, valor_icms_partilha_destino, valor_icms_pobreza, nfe_id)
  </read_first>
  <action>
    Estender `frontend/src/pages/ComparacaoFiscal.tsx` (sem quebrar o que o Plano 03-01 entregou).

    Filtro (CMP-03/D-08): adicionar estado `const [somenteDivergentes, setSomenteDivergentes] = useState(false)` e um controle de toggle (Button/Switch com ícone, ex. `Filter` ou `AlertTriangle` do lucide) rotulado "Só divergentes". Calcular `displayItems` com `useMemo(() => items.filter(item => !somenteDivergentes ? true : isDivergente(item)), [items, somenteDivergentes])`. Como `isDivergente` já retorna false para `fiscal_status !== 'ok'`, itens "Não calculado" nunca aparecem com o toggle ligado (D-10). A tabela do Plano 03-01 deve iterar sobre `displayItems` (não sobre `items` cru).

    Cards de resumo (CMP-04/D-09): acima da tabela, um grid `grid grid-cols-2 md:grid-cols-4 gap-2` de Cards pequenos derivados de `displayItems` via `useMemo`: Total itens (`displayItems.length`), OK (`itemBucket === 'ok'`), Divergentes (`=== 'divergente'`), Não calculados (`=== 'nao_calculado'`). Os cards refletem o filtro atual (respeitando `somenteDivergentes`).

    Dialog de detalhe (D-03/D-05/D-07): adicionar estado `selectedId` (string|null); linhas da tabela viram clicáveis (`cursor-pointer hover:bg-muted/50`, `onClick={() => setSelectedId(row.item_id)}`). Componente `DetalheItem({ id, onClose, authHeaders })` no padrão `DetalheNFe`: `useQuery` keyed por `['fiscal-comparison-item', id]` fetch de `/api/fiscal-comparison/${id}`, com estados loading/erro, dentro de `<Dialog open onOpenChange={onClose}><DialogContent className="max-w-3xl max-h-[85vh] overflow-y-auto">`. Reaproveitar `Secao` de ConsultaNFeSaidas.tsx. Criar um helper `LinhaComparativa({ label, esperado, calculado })` que renderiza 3 valores (Esperado | Calculado | Diferença) em `grid grid-cols-4` (label + 3 valores), com `bg-red-50`/`text-red-700` quando `pairDiff(esperado, calculado)` é `!= 0 && != null` (D-05/D-06).

    Seção 1 do Dialog "Comparação — Esperado vs. Calculado": 8 linhas `LinhaComparativa` (Base ICMS, Valor ICMS, Base ICMS-ST, Valor ICMS-ST, Base PIS, Valor PIS, Base COFINS, Valor COFINS) usando os pares esp_*/calc_* do detalhe.

    Seção 2 do Dialog "Só calculado (sem par no XML)" (D-07): usar `LinhaBRL` (só valor calculado, sem coluna esperado/diferença) para DIFAL (`valor_icms_partilha_destino`), FCP (`valor_icms_pobreza`), `percentual_difal`, e um conjunto de campos amigáveis extraídos de `full_result` (JSONB). Mapear pelos nomes de `FiscalResult` (oracle_fiscal.go) — escolher os campos de maior valor de auditoria (ex.: IBS UF/Município, CBS, alíquotas) com labels legíveis; para os demais campos crus de `full_result`, renderizá-los de forma genérica (chave → valor) é aceitável (mapeamento exato de labels é discretion). Quando o item está "Não calculado" (`fiscal_status !== 'ok'`), exibir aviso "Item ainda sem cálculo fiscal — nada a comparar." em vez das seções de valores.

    Resumo por nota (CMP-04/D-09): dentro do Dialog, mostrar contagem OK/Divergente/Não calculado dos itens da mesma nota (`nfe_id` do item selecionado), calculada client-side a partir do `items` já carregado (filtrar `items` por `nfe_id` e agrupar por `itemBucket`). Exibir com `Linha` numa `Secao title="Resumo da nota"`.

    Ao final do componente, renderizar `{selectedId && <DetalheItem id={selectedId} onClose={() => setSelectedId(null)} authHeaders={authHeaders} />}`.
  </action>
  <verify>
    <automated>npm --prefix frontend run build && npx --prefix frontend tsc --noEmit -p frontend/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix frontend run build` e `npx --prefix frontend tsc --noEmit -p frontend/tsconfig.json` passam sem erros
    - `grep -q "somenteDivergentes" frontend/src/pages/ComparacaoFiscal.tsx` verdadeiro e `displayItems` é usado no `.map` da tabela (não `items` cru)
    - `grep -q "fiscal-comparison/\${" frontend/src/pages/ComparacaoFiscal.tsx` verdadeiro (fetch de detalhe por id)
    - `grep -q "valor_icms_partilha_destino\|DIFAL" frontend/src/pages/ComparacaoFiscal.tsx` e `grep -q "valor_icms_pobreza\|FCP" frontend/src/pages/ComparacaoFiscal.tsx` verdadeiros (seção "Só calculado")
    - Cards de resumo derivam de `displayItems` (respeitam o filtro) e contam OK/Divergente/Não calculado + Total
    - Filtro "só divergentes" exclui itens "Não calculado" (via isDivergente que exige fiscal_status === 'ok')
  </acceptance_criteria>
  <done>Build/typecheck passam; a página tem toggle "só divergentes" funcional, cards de resumo por bucket que respeitam o filtro, e um Dialog de detalhe com as 8 comparações Base+Valor, a seção "Só calculado" (DIFAL/FCP/full_result) e o resumo por nota.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → API (`/api/fiscal-comparison/{id}`) | `id` do item vem do cliente (path param) — entrada não confiável |
| API → Postgres | QueryRow parametrizado, somente leitura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Elevation of Privilege / IDOR | GET /api/fiscal-comparison/{id} | mitigate | `WHERE i.id = $1 AND i.company_id = $2` — item de outra empresa retorna 404, não vaza dado cross-tenant |
| T-03-07 | Spoofing | rota de detalhe | mitigate | `withAuth(..., "")` exige JWT; company via `erpBridgeGetCompany`, nunca do cliente |
| T-03-08 | Tampering | query de detalhe | mitigate | parametrização `$1`/`$2`; id do path apenas como valor de bind |
| T-03-09 | Information Disclosure | error/full_result | mitigate | erro genérico via jsonErr + log interno; `full_result` já é dado da própria empresa (escopado), sem PII de terceiros |
| T-03-SC | Tampering | npm/go installs | accept | Nenhum pacote novo — reusa lucide-react, shadcn Dialog, react-query já presentes |
</threat_model>

<verification>
- `go -C backend build ./...` + `go -C backend vet ./handlers/` passam.
- `npm --prefix frontend run build` + `npx --prefix frontend tsc --noEmit -p frontend/tsconfig.json` passam.
- Verificação visual completa fica no checkpoint do Plano 03-03.
</verification>

<success_criteria>
- Filtro "só divergentes" mostra apenas itens divergentes, excluindo "Não calculado" (CMP-03).
- Cards de resumo global (Total/OK/Divergente/Não calculado) respeitando o filtro + resumo por nota no Dialog (CMP-04).
- Dialog de detalhe com comparações Base+Valor e seção "Só calculado" (DIFAL/FCP + full_result) por item (D-05/D-07).
</success_criteria>

<output>
Create `.planning/phases/03-visual-comparison-screen/03-02-SUMMARY.md` when done
</output>
</content>
</invoke>
